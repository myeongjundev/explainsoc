# 진행 기록

## 2026-09-25 — 제출 준비

- 사용자 요청으로 제출문 ②를 기록된 사용자 결정에서 1인칭 글과 한 줄로 다듬었다. 사용자가 기억과 맞는지 확인한 뒤 제출한다.
- card-5의 짧은 확인 방법(BRB-C13)과 제출 목록이 V7(`4489803`) 기준으로 남아 있어, 공개 주소를 쿠키·로그인 없는 새 브라우저 컨텍스트로 열어 확인한 현재 문구·값으로 다시 썼다.
- card-5 맨 아래 "제출 준비"에 제출 폼에 옮길 글(결과물 URL·ZIP, 12번 연결, 짧은 확인 방법, 세 줄)과 완주 체크리스트 대조표를 두었다.
- 새 컨텍스트의 콘솔 오류는 모두 이 PC의 스크립트 삽입 프로그램에서 왔고 앱의 보안 정책이 막았다. 앱 자체 오류는 없었다.

## 2026-09-25 — README 포트폴리오판과 제출문 ② 초안

- 사용자가 제출문 ② 작성과 README를 포트폴리오에 맞게 꾸미는 것을 요청했다.
- 제출문 ②: `DESIGN.md`는 ②를 사용자가 쓰게 정했으므로, 이 기록과 card-5에 남은 **사용자 결정만** 날짜별로 모은 초안을 card-5에 두고 "사용자가 자기 말로 고친 뒤 제출"이라고 표시했다.
- README 머리를 다시 짰다: 배지, 공개 사이트 바로 가기, 첫 화면·결론 카드 화면, 30초 요약, 화면으로 보기(질문·소개서 판독·실험실·휴대폰), 처음 보는 사람이 혼자 쓰게 되기까지(관찰 → 바꾼 것, 동기의 한 줄), 기술과 만든 방식. 저장소에만 있는 화면·문서는 절대 주소로 걸어 ZIP 안에서도 열린다. README용 화면 두 장은 `evidence/readme/`에 있다. 논문 수치·규칙·앱 코드는 바꾸지 않았다.
- README가 실행 묶음에 들어가므로 제출 기준을 `f34f7d5`로 옮겼다. `explainsoc-f34f7d5.zip`(198,111 bytes, 71개 파일), SHA-256 `9b30bbd2ca626065cb025262fd92775c93b227b619ddfe158b23a724f4903e57`. 두 번 생성한 해시가 같고, 저장소 밖 빈 폴더에서 `npm ci`(취약점 0)·Vitest 152개·빌드·안전 검사 11개·Playwright 35개(26개 건너뜀)를 다시 통과했다. 배포 run 36029761981 성공, 공개 자산은 `3ae1312`와 같다.
- 사용자 요청으로 12번 사이트 13번 카드의 요약과 사례 노트를 README 30초 요약에 맞췄다(`about` `fbf5948`). 검증 칸에 처음 보는 사람 관찰 여섯 번과 동기의 휴대폰 사용 결과를 넣었다. 그림·링크·테스트 수는 그대로다. T12 checks·Pages 성공, 공개 사이트에서 새 문장을 확인했다.

## 2026-09-25 — 독립 검토 후보 2는 하지 않음, BRB-C11 완료

- 공개 사이트를 390×844·375×812·360×740에서 재어 보니 `업체에 물을 질문 보기`는 결과 진입 직후 y=1,260px(360×740은 1,303px)로 이전 검토 값과 같았다. 이유 두 줄이 886px에서 끝나 첫 화면 안에 단추를 넣으려면 이유보다 위로 올려야 한다. 단추를 누르면 초점이 질문 목록으로 가고 첫 질문과 "왜 묻나요"가 한 화면에 보인다.
- 사용자가 동기에게 보여 준 결과를 "혼자 잘하던데"라고 전했고, 양식으로 휴대폰 크롬·1분 안에 목적 설명 예·도움 없이 업체 질문 도달 예를 알려 주었다(card-5 여섯 번째 관찰). 인계 문서 5절대로 현재 배치를 유지하고 코드는 바꾸지 않았다. 막힌 곳은 없었다. 처음에는 기억하지 못했던 이름 없는 한 줄을 사용자가 같은 날 전해 주어 card-5에 그대로 옮기고 BRB-C11을 완료로 표시했다. 남은 사용자 몫은 제출문 ②(BRB-C14), 시크릿 창 확인과 제출이다.

## 2026-09-25 — V10 독립 검토 수정 후보 1: 새 공격 "포함"과 "따로 시험"을 결론에서 가름 (배포)

- 근거: `planning/CODEX-V10-INDEPENDENT-REVIEW-HANDOFF.md` 4절. 사용자가 후보 1만 구현·검증하고 후보 2(모바일 질문 단추 위치)는 고치지 말라고 했다.
- 원인: `src/domain/conclusion.ts`의 `conditionReason()`이 `split === 'unseen' || unseenIncluded === 'yes'`를 같은 뜻으로 읽었다. 분할 모름·섞어서 나눔·다른 방식에 "학습 때 없던 공격이 들어 있었다: 예"만 답해도 결론 제목이 "처음 보는 공격으로 시험한 숫자가 있습니다", 이유가 "따로 시험한 숫자입니다"가 됐다. 고치기 전 재현 테스트 3개가 이 결과로 실패했다.
- 바꾼 것: "따로 시험"은 분할 답(`unseen`)만 말한다. 포함 답만 예이면 결론은 `early`(광고 숫자만으로는 아직 믿기 이릅니다)이고, 이유는 "학습 때 없던 공격이 시험에 들어 있었다는 답은 있지만, 그 공격만의 성능을 따로 확인한 숫자인지는 알 수 없습니다."이다. 섞어서 나눈 경우에는 그 사실을 앞에 붙인다. 받은 숫자 줄은 그대로 두고 새 공격만의 수치로 잇지 않는다.
- 그대로인 것: `unseen`+예/모름/고르지 않음은 전처럼 따로 시험으로 읽고, `unseen`+아니오는 모순으로 남는다. 두 논문 예시의 결론, 입력 형식·사례 파일, R01~R15, 질문 생성, 보안 경계는 바꾸지 않았다.
- 검사: 회귀 테스트 6개를 `tests/unit/conclusion.test.ts`에 더했다. 안전 검사 11개, Vitest 152개, 빌드, Playwright 35개(증거 캡처 26개 건너뜀) 통과. 한 장짜리 폼으로 random+예·모름+예·unseen+예를 1280·390 폭에서 넣어 결론 제목·이유·질문이 같은 근거 수준을 말하는지 화면에서 확인했다(임시 스크립트, 저장소에 남기지 않음).
- 사용자 승인("후보1을 커밋하고 배포해")으로 `v10-fix1` 브랜치에 커밋하고 `main`에 fast-forward 병합했다(`3ae1312`). GitHub Actions [run 36021634394](https://github.com/myeongjundev/explainsoc/actions/runs/36021634394)의 검사와 Pages 배포가 성공했다. 공개 `index-DfnmrLxk.js`·`index-DhJb78PJ.css`의 SHA-256이 로컬 빌드와 같고(공개 JS에 새 문장이 들어 있다), 공개 주소 브라우저 검사 35개도 통과했다.
- 제출 기준이 `3ae1312`로 바뀌었다. 실행 묶음은 `explainsoc-3ae1312.zip`(196,359 bytes, 71개 파일), SHA-256 `e0429ac2e1696c3fd95b791e11a279ed776bdf0feabe46e8d9f1d0bb6c511ac6`이다. 두 번 생성한 해시가 같고, 저장소 밖 빈 폴더에서 `npm ci`(취약점 0)·Vitest 152개·빌드·안전 검사 11개·Playwright 35개(26개 건너뜀)를 다시 통과했다.
- 12번 대표 카드의 검증 문장을 `about` 커밋 `b4084d4`에서 152개로 고쳤다. 그림은 그대로다.
- 후보 2는 구현하지 않았다. 인계 문서 5절대로 신규 사용자 관찰에서 문제가 보일 때만 고친다. BRB-C11은 사람의 수행을 기다린다.

## 2026-09-24 오후 — V10 품질 개선판 배포

- 사용자가 `planning/CLAUDE-V10-IMPROVEMENT-PROMPT.md`를 주었다. 요구대로 코드부터 고치지 않고 화면을 재어 분석을 먼저 보고했고, 사용자가 수정 항목 7개와 실험실 카드를 밝은 테두리로 낮추는 안을 승인했다. 설계는 `DESIGN.md` 32절.
- 잰 사실: 1280×800에서 첫 화면에 누를 단추가 없었다(주 단추 863px). `예시로 바로 보기`가 첫 화면의 99.88% 이야기와 다른 예시 A를 열었다. 결론 카드 아래 검토 요약이 결론을 되풀이했다. 질문 장으로 가도 초점이 단추에 남았고 질문에 이유가 없었다.
- 바꾼 것: 첫 화면 순서와 크기(네 화면 크기에서 스크롤 없이 주 단추), 기본 예시를 99.88%(예시 B)로 하고 예시 A는 `다른 예시`로, 검토 요약 한 줄, 질문 초점 이동과 상태·왜 묻나요, 비율·검토 파일 오류 안내, 실험실의 CICIDS2017 한 줄, README 재구성.
- 논문 수치, R01~R15, P01~P14, 보안 경계는 바꾸지 않았다. 새 기능은 넣지 않았다.
- 안전 검사 11개, Vitest 146개, 빌드, Playwright 35개 통과(증거 캡처 26개 건너뜀). 증거 화면은 현재 화면을 다시 찍고 `evidence/v10-*.png` 4장을 더했다.
- 사용자 승인("배포해")으로 `v10-polish`를 `main`에 fast-forward 병합했다(`5690d62`). GitHub Actions [run 35969412385](https://github.com/myeongjundev/explainsoc/actions/runs/35969412385)의 검사와 Pages 배포가 성공했다. 공개 `index-EYftpwNU.js`·`index-DhJb78PJ.css`의 SHA-256이 로컬 빌드와 같고, 공개 주소 브라우저 검사 35개도 통과했다.
- 제출 기준이 V10으로 바뀌었다. 실행 묶음은 `explainsoc-5690d62.zip`(195,103 bytes, 71개 파일), SHA-256 `bec1367329ca9fd3f8249d687c225075e6b95492bae590b28931e698c6b9d5ea`이다. 두 번 생성한 해시가 같고, 저장소 밖 빈 폴더에서 `npm ci`(취약점 0)·Vitest 146개·빌드·안전 검사 11개·Playwright 35개(26개 건너뜀)를 다시 통과했다.
- 12번 대표 카드의 검증 문장을 `about` 커밋 `029b56c`에서 146개·35개로 고쳤다. 그림(V9 결론 카드)은 V10에서도 같은 화면이라 그대로 뒀다. [T12 checks 35969652252](https://github.com/myeongjundev/about/actions/runs/35969652252)와 Pages 배포가 성공했다.
- 남은 일(사용자): ExplainSOC를 본 적 없는 ALEPH 동기 한 명에게 재시험(BRB-C11, 요청 문서 11절 양식), 제출문 ②(BRB-C14), 시크릿 창 확인과 제출.

## 2026-09-24 — V9 소개서 판독·결론 카드·논문 실험실 배포

- 동기들이 자기 논문으로 만든 앱을 본 사용자가 "더 좋고 풍부한 앱"을 원했고, 새 프로젝트가 아니라 T13 개선으로 `v9-brochure` 브랜치에서 진행했다. 설계는 `DESIGN.md` 31절.
- 비전공 지인의 반응 세 가지(첫 화면이 전문적이다, 결과를 보고 "그래서 뭐?", 보안 AI 광고를 본 적이 없다)를 `evidence/card-5.md`에 적었다. 시간·기기·세 행동이 없어 BRB-C11 완료로 처리하지 않았다.
- 바꾼 것:
  - 첫 화면: 시험 공부 비유로 다시 쓰고, 큰 단추는 하나로 줄이고, 짙은 논문 실험실 카드를 뒀다.
  - 소개서 판독: 붙여 넣은 문장 위에 규칙으로 표시·메모·논문 근거·질문을 단다.
  - 결론 카드: 결론 · 왜? · 그래서?와 논문의 두 시험 막대를 둔다.
  - 논문 실험실: 원고 V-2~V-5를 실험 네 개로 직접 바꿔 본다(근거 P11~P14).
  - 판독 규칙: R15(설명 가능한 AI 주장)를 더했다.
  - 이동: 판독 메모·결론 카드에서 실험 1·4로 바로 간다.
- 사용자가 정한 것:
  - 결론 카드만 넣는다(질문형 제목·99.88% 예시 시작은 넣지 않는다).
  - 보여 줄 상대는 전문가나 어느 정도 지식이 있는 사람으로 본다.
  - 디자인을 다시 잡는다.
  - 첫 화면 실험실 입구를 눈에 띄게 한다.
- 전문가용 첫 화면 안은 제안만 했고 넣지 않았다.
- 검사 결과:
  - 로컬: 안전 검사 11개, Vitest 141개, 빌드, Playwright 31개 통과(증거 캡처 22개 건너뜀).
  - 증거 화면: 현재 화면을 다시 찍고 `evidence/v9-*.png` 5장을 더했다. V3~V8 파일은 그대로 뒀다.
- 배포:
  - 사용자 승인("반영해")으로 `v9-brochure`를 `main`에 fast-forward 병합했다(`d14171e`).
  - GitHub Actions [run 35947858700](https://github.com/myeongjundev/explainsoc/actions/runs/35947858700)의 검사와 Pages 배포가 성공했다.
  - 공개 `index-CFObKeM9.js`·`index-D8QQMwkU.css`의 SHA-256이 로컬 빌드와 같다. 공개 주소 브라우저 검사 31개도 통과했다.
- 제출 기준이 V9로 바뀌었다.
  - 실행 묶음: `explainsoc-d14171e.zip`(190,431 bytes, 71개 파일), SHA-256 `3ce9908d2d26d4d2a2c593f01903b8f07c69c8f8be8fccc0b14390b804479299`.
  - 두 번 생성한 해시가 같다.
  - 저장소 밖 빈 폴더에서 `npm ci`(취약점 0)·Vitest 141개·빌드·안전 검사 11개·Playwright 31개(22개 건너뜀)를 다시 통과했다.
- 12번 대표 카드:
  - `about` 커밋 `0f11a11`에서 V9 결론 카드 그림(768×705)과 141개·31개 검증 문장으로 바꿨다.
  - [T12 checks 35948187951](https://github.com/myeongjundev/about/actions/runs/35948187951)와 Pages 배포가 성공했다. 공개 그림 SHA-256이 로컬과 같다.
- 남은 일(사용자): 앱의 대상에 가까운 처음 보는 사람(예: 학원 동기)에게 V9 재시험(BRB-C11), 제출문 ②(BRB-C14), 시크릿 창 확인과 제출.

## 2026-09-23 저녁 — V8 밝은 문서형 화면과 두 입력 방식 배포

- 학원 PC에서 내부 UI를 밝은 문서형으로 전면 개편하고 입력을 "처음은 한 화면씩, 고칠 때는 한 페이지"로 바꿨다(`4bb1a7a`). 경위와 변경 목록은 `planning/HANDOFF-2026-09-23-UI.md`, 설계는 `DESIGN.md` 30절.
- 집 PC에서 두 흐름(논문 예시 → 결과 세 장, 내 성능표 → 질문 6개 → 결과 → 입력 수정)을 1280px·390px로 끝까지 눌러 가로 넘침·페이지 오류가 없음을 확인했다.
- 사용자가 결과 1장의 중복 패널을 하나로 합치기로 정했다. 평가 근거 지도(3칸)를 없애고 그 패널에만 있던 `평가 조건 확인` 단추, R12 모순 안내, "모름은 실패가 아니다" 문장을 근거 확인표(6칸)로 옮겼다(`169d89e`). 판독 규칙·수치·JSON·보안 경계는 그대로다.
- 안전 검사 11개, Vitest 99개, 빌드, Playwright 27개 통과(증거 캡처 12개 건너뜀). 증거 화면은 현재 화면 8장을 다시 찍고 `evidence/v8-*.png` 5장을 더했다.
- 사용자 승인으로 `ui-redesign`을 `main`에 fast-forward 병합했다(`ad45f7c`). GitHub Actions [run 35859205922](https://github.com/myeongjundev/explainsoc/actions/runs/35859205922)의 검사와 Pages 배포가 성공했다. 공개 `index-q6bbu4Ws.js`·`index-DC9q-JfQ.css`의 SHA-256이 로컬 빌드와 같고, 공개 주소 브라우저 검사 27개도 통과했다.
- 제출 기준이 V8로 바뀌었다. 실행 묶음은 구현 커밋 `169d89e`의 `explainsoc-169d89e.zip`(151,986 bytes, 64개 파일), SHA-256 `87e956cee7e05486c69d28e56b3bcf4e610e41f5caee254ba47baddab15acb6d`이다. 두 번 생성한 해시가 같고, 저장소 밖 빈 폴더에서 `npm ci`(취약점 0)·Vitest 99개·빌드·안전 검사 11개·Playwright 27개(17개 건너뜀)를 다시 통과했다.
- 12번 대표 카드를 `about` 커밋 `bbc9424`에서 V8 검토 요약 그림(343×483)과 99개·27개 검증 문장으로 바꿨다. [T12 checks 35859976984](https://github.com/myeongjundev/about/actions/runs/35859976984)와 Pages 배포가 성공했다. 사이트 공통 그림 덮개(아래로 어두워지는 그라데이션과 `04` 번호)는 바꾸지 않았다.
- 남은 일(사용자): 새로운 처음 보는 사람에게 V8 재시험(BRB-C11), 제출문 ②(BRB-C14), 시크릿 창 확인과 제출.

## 2026-09-23 — 두 번째 관찰(결과 밀집) 반영, V7 한 장씩 읽는 결과

- 앱을 처음 본 두 사람에게서 "버튼을 누른 뒤 보이는 것들이 너무 밀집해 보기 힘들다", "캐릭터나 그래프, 이미지가 있으면 좋겠다"는 관찰을 받았다. 시간·기기·세 행동·직접 인용은 받지 못해 BRB-C11 완료로 처리하지 않고 evidence/card-5.md에 그대로 적었다.
- Claude가 같은 화면을 재어 뒷받침했다: 1280px 결과가 5.2화면 길이에 패널 16개를 세 열로 동시에 펼쳤다.
- V7에서 결과를 주장과 근거 / 판독 / 다음 행동 세 장으로 나눠 모든 폭에서 한 번에 한 장만 보이게 했다. 장 아래에 이전 / 다음과 N / 3 위치를 두었고, 잘못 적은 칸 수는 브리핑에서 먼저 알린다. 인쇄는 보고 있는 장과 무관하게 검토표 전체를 낸다.
- 받은 혼동행렬을 실제 비율 막대로 그렸다(공격 220,788건 중 탐지 160건 · 0.072%, 정상 375,518건 중 오탐 86건 · 0.023%). 축을 자르거나 최소 너비를 주지 않고, 보이지 않는 값은 눈금과 글로 알린다. 캐릭터·삽화는 사용자와 상의해 넣지 않았다.
- 설계는 DESIGN.md 29절. 구현 커밋 `4489803`; GitHub Actions 검사와 Pages 배포 성공. 공개 `index-BkE6dFvd.js`·`index-CJSslC0U.css`의 SHA-256이 로컬 빌드와 같고, 공개 주소 브라우저 검사 26개도 통과했다.
- 안전 검사 11개, Vitest 95개, Playwright 26개, 증거 캡처 12개 통과. 실행 묶음은 `explainsoc-4489803.zip`(145,603 bytes, 64개 파일), SHA-256 `3eca59e1007b9767ef5bd4f0953f098feb048ced5fc34be4aa5623e0536c53dc`이다. 두 번 생성한 해시가 같고, 저장소 밖 빈 폴더에서 설치·테스트·빌드·안전 검사 11개·Playwright 26개를 다시 통과했다.

## 2026-09-22 — 첫 비전공자 관찰에서 목적 전달 실패, V6 명료화

- 사용자가 처음 보는 비전공자 한 명에게 공개 주소를 보여 줬다. 전달받은 결과는 첫 화면에서 무엇을
  하는 사이트인지 이해하지 못했다는 것이었다. 시간·기기·세 행동 완주 여부는 기록되지 않아 BRB-C11
  완료로 처리하지 않았다.
- 첫 화면의 문제를 기능 부족이 아니라 언어 문제로 판단했다. 제목에 `보안 AI`를 넣고, 광고 숫자를
  확인해 판매 업체에 물을 질문을 만드는 도구라는 쉬운 설명과 실제 사용 상황을 추가했다.
- 세 단계도 `숫자 입력 → 모르는 시험 조건 답변 → 업체 질문 완성`으로 바꿨다. 수치·논문·R01~R14·
  계산·결과·보안 경계는 바꾸지 않았다. 설계는 `DESIGN.md` 28절이다.
- 구현 커밋 `b5384eee193add96acbacd1f59d2dbf5d06a9577`; GitHub Actions
  [run 35732418454](https://github.com/myeongjundev/explainsoc/actions/runs/35732418454)의 검사와 Pages 배포가
  성공했다. 공개 주소에서 기능·품질 26개와 증거 캡처 9개를 통과했고 배포 자산 SHA-256도 로컬과 같다.
- 새 실행 묶음은 `explainsoc-b5384ee.zip`(140,962 bytes, 63개 파일), SHA-256
  `5cda7ec171d36a352dd3cd33f5b17a1092453a8356288862d4200bd189b8c87b`이다. 저장소 밖 새 임시 폴더에서
  설치·안전 검사 11개·Vitest 92개·빌드·미리보기 HTTP 200·Playwright 26개를 다시 통과했다.
- 12번 대표 카드의 한 문장도 `about` 커밋 `64945b4`에서 쉬운 말로 맞췄다.
  [T12 checks 35732677557](https://github.com/myeongjundev/about/actions/runs/35732677557)와
  [Pages 35732676543](https://github.com/myeongjundev/about/actions/runs/35732676543)이 성공했다.

## 2026-09-22 — 대표 앱 V5 결과 우선 브리핑과 제출본 재검증

- 기능 범위는 늘리지 않고 결과의 읽기 순서를 다시 설계했다. `Decision Brief`가 현재 회차, 확인 필요·해석
  주의·다음 질문 수와 첫 질문을 결과 맨 앞에 보여 준다. 3열 보드는 독립 카드로 정리하고, 모바일은 근거·판독·
  다음 행동을 한 장씩 전환한다. 회차 입력은 결과 뒤의 펼침 영역으로 옮겼다.
- 구현 커밋 `7d0a105f8b14dd05e635a56dce261e87ea0b6933`; GitHub Actions
  [run 35728715004](https://github.com/myeongjundev/explainsoc/actions/runs/35728715004)의 검사와 Pages 배포가
  성공했다. 공개 `index-WXO94w-9.js`·`index-i0wfD4Ar.css`의 SHA-256은 로컬 빌드와 같다.
- 안전 검사 11개, Vitest 92개, Playwright 기능·품질 26개와 증거 캡처 9개를 통과했다. 공개 주소도 새
  브라우저 컨텍스트에서 26개와 9개를 모두 통과했다.
- 실행 묶음은 `explainsoc-7d0a105.zip`(140,605 bytes, 63개 파일), SHA-256
  `1b2854d4a847a5606711b4ba020adc6595ca698f5aefc96a0bdc668f7c21fe92`이다. 저장소 밖 새 임시 폴더에서
  `npm ci`부터 안전 검사·Vitest·빌드·미리보기 HTTP 200·Playwright까지 다시 통과했다.
- 12번 포트폴리오 카드도 `about` 커밋 `e53e2c3`에서 V5 Decision Brief 그림과 92개·26개 검증 문장으로
  갱신했다. [T12 checks 35729849396](https://github.com/myeongjundev/about/actions/runs/35729849396)와
  [Pages 35729848220](https://github.com/myeongjundev/about/actions/runs/35729848220)이 성공했고, 공개 이미지
  SHA-256도 로컬과 같다.

## 2026-09-22 — 집 PC 최신화와 V4 제출 직전 재검증

- 원격 `main`의 학원 작업 14개 커밋을 `8aa382a`까지 fast-forward했다. 충돌과 로컬 미반영 변경은
  없었다.
- Node.js 24.15.0에서 `npm ci`와 `npm run verify`를 다시 실행했다. 취약점 0개, 안전 검사 11개,
  Vitest 91개, 프로덕션 빌드, Playwright 25개가 모두 통과했다(증거 캡처 9개 제외).
- 공개 주소도 로그인·쿠키·저장소가 없는 새 브라우저 컨텍스트에서 Playwright 25개를 통과했다.
- `explainsoc-3220867.zip`을 다시 만들었다. 크기 136,265 bytes, 파일 62개, SHA-256
  `8f0ef71046c9bd643fa1d18e3c1bb3c37ca57ca6b1657b1b2193344c75128a1e`로 학원 PC 기록과 같다.
- `evidence/card-5.md`의 AI 작업 초안을 V4 사실(R01~R14, Vitest 91개, 브라우저 25개)로 고쳤다.
- 남은 필수 작업은 실제 처음 보는 사람 시험(BRB-C11), 사용자가 직접 쓰는 판단 한 줄(BRB-C14),
  제출 직전 새 시크릿 창 확인과 제출이다. 이 둘은 지어내지 않고 비워 둔다.

## 2026-09-22 — 대표 앱 V4 성능 주장 수사 보드

- 첫 화면을 `99.88% 주장 → 두 시험 → 220,788건 중 160건 탐지`가 한 장면에서 보이는 Claim Autopsy로
  바꾸고, 모바일에서는 CTA 바로 다음에 대표 장면이 시작되게 했다.
- 결과를 `주장과 근거 / 판독 / 다음 행동` 3열 보드로 재구성하고, 점수가 아닌 6항목 근거 지도와
  같은 시험·미확인·별도 시험 타임라인 분기를 추가했다.
- 검토표는 다음 회의 질문을 먼저 보여 주는 Decision Dossier 앞장과 펼칠 수 있는 전체 기록으로 나눴다.
  인쇄에서는 앞장과 전체 기록만 나오도록 별도 회귀 검사를 추가했다.
- 구현 커밋 `3220867`; GitHub Actions [run 35700989600](https://github.com/myeongjundev/explainsoc/actions/runs/35700989600)의
  검사와 Pages 배포가 성공했다. 공개 `index-CfDxTmKS.js`·`index-2ZwK-MBR.css`의 SHA-256은 로컬과 같다.
- 안전 검사 11개, Vitest 91개, Playwright 25개와 증거 화면 9개를 통과했다. 공개 주소도 같은 25개를
  통과했고 375px 가로 넘침, WCAG 2.2 AA, 키보드, 모션 감소, 인쇄를 포함한다.
- 실행 묶음은 `explainsoc-3220867.zip`(136,265 bytes, 62개 파일), SHA-256
  `8f0ef71046c9bd643fa1d18e3c1bb3c37ca57ca6b1657b1b2193344c75128a1e`이다. 두 번 생성한 해시가 같고,
  저장소 밖 빈 폴더에서 설치·빌드·안전 검사·Vitest·Playwright를 다시 통과했다.
- 12번 포트폴리오 카드도 `about` 커밋 `5584650`에서 V4 Claim Autopsy 그림, 수사 보드 판단,
  브라우저 테스트 25개 문장으로 갱신했고 T12 검사와 Pages 배포가 성공했다.

## 2026-09-22 — V3 독립 리뷰 R1~R5·L1~L3 반영

- 같은 시험이 `아니오`면 규칙 소멸을 `해결됨`으로 부르지 않고 이전 시험과 이번 별도 시험의 판독을
  나란히 표시한다. `예`는 확정 비교, `모름`은 R14를 둔 임시 비교다.
- 다음 회차는 직전 질문 상태·메모를 편집 초깃값으로 이어받되 이전 회차 스냅샷은 바꾸지 않는다.
  검토표의 회차 이력에도 당시 상태와 메모를 남긴다.
- 후속 1·2단계에 새로 받은 자료만 적는다는 안내와 이전 값을 표시하고, 명시적 `모름`이 이전 값을
  바꾼다는 것을 입력 옆에서 알린다.
- 검토표·자료 요청서의 R·P 코드에 규칙 제목과 원고 절·근거 제목을 붙였다. 같은 시험 선택 칸을
  전체 너비로 넓히고 파일 열기 포커스를 보이는 버튼에 표시했다.
- 손수 고친 JSON의 후속 `sameTrial: null`과 공백 이름을 거부하고, V3 증거 화면을 현재 코드로 다시 찍었다.
- 로컬 `npm run verify` 통과: 안전 검사 11개, Vitest 91개, 프로덕션 빌드, Playwright 24개
  (증거 캡처 9개 제외). 구현 설계는 `DESIGN.md` 25절이다.
- 구현 커밋 `679add3`; GitHub Actions [run 35698164859](https://github.com/myeongjundev/explainsoc/actions/runs/35698164859)의
  검사와 Pages 배포가 성공했다. 공개 `index--yn4oNyf.js`·`index-D1Vo0e1F.css`의 SHA-256은 로컬 빌드와 같고,
  공개 주소 브라우저 검사 24개도 통과했다.
- 현재 실행 묶음은 `explainsoc-679add3.zip`(130,786 bytes, 61개 파일), SHA-256
  `80650a6729c912dadd071adb87384d59e253660a82d4296cefa0a0503ecffdbd`이다. 두 번 생성한 해시가 같고,
  저장소 밖 빈 폴더에서 설치·빌드·안전 검사 11개·Vitest 91개·Playwright 24개를 다시 통과했다.

## 2026-09-22 — 대표 앱 V3 초기 로컬 구현·검증

- 한 번의 정적 판독을 `최초 주장 → 공급자 답변 → 변화 비교`의 회차형 PoC 사례 작업대로 확장했다.
- 같은 시험 여부가 확인되지 않은 후속 자료에는 R14를 남기고, `해결됨 / 남음 / 새로 생김`은 규칙 ID 차이로만 계산한다.
- 사용자가 직접 누를 때만 숫자·답변 메모·출처를 담은 버전 1 JSON을 내려받고 다시 연다. 1 MiB 제한, 엄격한 키·자료형·길이 검증, 최대 20회차를 적용했다.
- 공급자 자료 요청서, 회차 요약을 포함한 인쇄용 검토표, 작업대 바로 가기, 키보드·터치 용어 풀이를 추가했다.
- 로컬 `npm run verify` 통과: 안전 검사 11개, Vitest 85개, 프로덕션 빌드, Playwright 23개 실행(증거 캡처 6개 제외). V3 JSON 왕복과 2회차 차이 비교도 실제 브라우저로 검증했다.
- 구현 커밋 `8696eb9`; GitHub Actions [run 35682006918](https://github.com/myeongjundev/explainsoc/actions/runs/35682006918)의 검사와 Pages 배포가 성공했다.
- 공개 주소는 로컬 빌드와 같은 `index-CebDU8ns.js`·`index-DVlAH9ql.css`를 제공했고 두 자산의 SHA-256도 일치했다. 공개 주소 브라우저 검사 23개도 통과했다.
- 당시 실행 묶음은 `explainsoc-8696eb9.zip`(126,324 bytes, 60개 파일), SHA-256
  `1a3e43624f1a3b18e161749ca6776d3f58c3b19c13c9d049ef3b7a3e5afb1a27`이다. 같은 커밋에서 두 번 만들어
  해시가 같았고, 저장소 밖 빈 폴더에서 `npm ci` → 빌드 → 안전 검사 11개 → Vitest 85개 → Playwright
  23개를 모두 통과했다(증거 캡처 6개 제외).

## 2026-09-22 — 대표 앱 V2 릴리스

- 결과 화면을 PoC 검토 작업대로 바꾸고, 평가 근거 지도, 질문별 상태·메모, 검토표 전체 복사·인쇄,
  지표·혼동행렬 불일치(R11), 평가 조건 모순(R12), 기타 분할 질문(R13)을 추가했다.
- 구현 커밋 `720dc76`; GitHub Actions [run 35675622887](https://github.com/myeongjundev/explainsoc/actions/runs/35675622887)의
  검사와 Pages 배포가 성공했다. 공개 주소의 자산 해시가 로컬 빌드와 같고, 공개 주소 브라우저 검사는 22개 통과했다.
- 새 실행 묶음은 `explainsoc-720dc76.zip`(112,221 bytes, 54개 파일), SHA-256
  `7dc9812c4528ddd47f6354ebcb359686bbd8f81989b14914399c242f213503c5`이다. 두 번 생성해 해시가 같았고, 새 임시 폴더에서
  `npm ci` → 안전 검사 → 80개 테스트 → 빌드 → 브라우저 22개를 전부 통과했다.
- 이 묶음이 당시 기존 `explainsoc-9d71d39.zip`을 대체한 V2 제출본이었다. 현재 제출본은 맨 위 V6 기록을 따른다.

## 2026-09-22 — 트러블슈팅 기록과 학원 인계

- 포트폴리오용 트러블슈팅 기록 `TROUBLESHOOTING.md`를 저장소 맨 위에 두고 README 첫머리에서 링크했다
  (`9d71d39`). 검사 환경을 오염시킨 HTTPS 가로채기, 반올림 숫자의 역산, 재현 가능한 ZIP, 내보내는 산출물 검사,
  화면 폭·접근성, 라이선스 고지, Node 버전, 포트폴리오 카드 그림, 두 PC 작업 순서를 상황·원인·해결·결과로 적었다.
- README가 바뀌어 제출할 묶음이 `explainsoc-9d71d39.zip`(SHA-256 `cb6cf566…ca752c4a`)이 됐다. 새 임시 폴더에서 실행
  세 줄, 검사 11개, 테스트 75개, 브라우저 20개가 통과했고, CI 검사·배포도 성공했다. 배포본 자산은 그대로다.
- 학원 PC에서 이어 갈 일을 `planning/HANDOFF-2026-09-22.md`에 적었다. ZIP은 저장소에 없으므로 학원 PC에서
  `npm run release:zip -- 9d71d39`로 다시 만든다.

## 2026-09-22 — 카드 5와 V1 최종 상태 (Claude 구현 멈춤)

인계 문서 6절대로 카드 5와 공개 URL 검증까지 마치고 여기서 멈춘다. 남은 것은 사람이 해야 하는 일뿐이다.

| 카드 | 커밋 | 증거 |
|---|---|---|
| 5 포트폴리오와 제출 | `3dd6274` `2e7bd73` (인용 수정) · `2e00f55` (MIT) · 12번 `about` `1dbbbfc` `ea77b4e` | `evidence/card-5.md` |

- **12번 대표작 연결(BRB-C10):** `t13-app`을 공개 카드로 바꾸고 이름·한 문장·사례 노트·그림·링크를
  채웠다. 12번 테스트·반복 검사·릴리스 검사·링크 검사 통과, 공개 사이트에서 확인했다.
- **전체 제출물 실명 검사(BRB-C12):** 앱과 README의 데이터셋 인용에 저자 세 명의 실명이 있어 빼고
  DOI로 가리키게 했다. 제출할 묶음이 바뀌었다.
- **라이선스:** 사용자가 MIT로 정했다. `LICENSE`를 넣고 묶음을 다시 만들었다(`2e00f55`).
- **제출할 것:** 결과물 URL https://myeongjundev.github.io/explainsoc/ · 실행 묶음
  `explainsoc-9d71d39.zip` (SHA-256 `cb6cf566…ca752c4a`, 로컬 `release/`) · 짧은 확인 방법과 제출문
  세 줄 초안은 `evidence/card-5.md`.
- **최종 검증:** 로컬 `npm run verify` 통과(안전 검사 11 · 단위·컴포넌트 75 · 빌드 · 브라우저 20),
  CI 검사·배포 성공, 공개 주소 브라우저 테스트 20개 통과, 최종 묶음 새 임시 폴더 실행 통과, 제출할 수
  있는 URL 7개 모두 로그인 없이 200.
- **완료 정의에서 아직 사실이 아닌 두 줄:** "사용자는 설명 없이 세 행동을 완주할 수 있다", "논문
  예시의 서명 장면이 60초 안에 전달된다"는 사람이 써 봐야 확인된다. 자동 테스트로는 통과했다.

## 남은 일 — 사용자 (V5에도 계속 해당)

1. **처음 보는 동료 한 명(BRB-C11)과 사람이 잰 60초.** 공개 주소만 주고 설명 없이 써 보게 한 뒤, 걸린
   시간·막힌 곳·이름 없는 한 줄을 `evidence/card-5.md`의 양식에 적는다.
2. **제출문 ② `내가 직접 판단한 일`(BRB-C14).** 사용자가 직접 쓴다. ①·③은 사실 초안이 있다.
3. **제출.** 결과물 URL과 `explainsoc-b5384ee.zip`(다른 PC에서는 `npm run release:zip -- b5384ee`로 다시 만든다),
   짧은 확인 방법, 세 줄. 자세한 순서는 `planning/HANDOFF-2026-09-22.md`.

## 2026-09-22 — 카드 1~4 구현 (Claude)

- 승인된 설계(`planning/DESIGN.md`)와 인계 문서(`planning/CLAUDE-HANDOFF.md`)대로 카드 1~4를
  구현하고, 카드마다 코드·테스트·증거를 함께 커밋해 푸시했다.

  | 카드 | 커밋 | 증거 |
  |---|---|---|
  | 1 앱 문장과 세 행동 | `a90e1fc` | `evidence/card-1.md` |
  | 2 첫 흐름 | `fee6025` | `evidence/card-2.md` |
  | 3 데이터와 비밀값 | `9857a54` | `evidence/card-3.md` |
  | 4 공개와 재현 | `665e819` | `evidence/card-4.md` |

- 공개 주소 https://myeongjundev.github.io/explainsoc/ — GitHub Actions가 안전 검사·테스트·빌드·
  의존성 감사·브라우저 테스트를 모두 통과한 커밋만 Pages에 배포한다. 공개 주소에 같은 브라우저
  테스트 20개를 돌려 통과했다.
- 실행 묶음 `explainsoc-25d68ce.zip` (SHA-256 `6986d64c…d3585134`)을 새 임시 폴더에 풀어
  `npm ci`·`npm run build`·`npm run preview`와 검사 전부를 확인했다.
- 사용자가 정할 것: 소스 코드 라이선스. 지금은 README에 미정으로 적혀 있다. 정하면 LICENSE를 넣고
  ZIP을 다시 만든다.

## 2026-09-21 — ExplainSOC 설계 승인과 Claude 구현 인계

- 사용자가 Codex 추천 방향으로 진행하도록 승인했다.
- `planning/DESIGN.md`에 ExplainSOC의 카드 1, 60초 핵심 경험, R01~R10, P01~P10,
  검증·배포·증거 기준을 확정했다.
- 첫 화면 제목은 `그 99%, 무엇을 시험한 점수입니까?`, 제품 설명은 `성능표에 없는 질문`으로
  확정했다. 9종 대 3종 정적 그림을 포함하고 공개 URL과 ZIP을 모두 제출한다.
- 구현은 `planning/CLAUDE-HANDOFF.md`를 기준으로 Claude에게 인계한다. 카드별 코드·테스트·증거를
  함께 커밋하고, 설계 밖 기능은 사용자 승인 없이 추가하지 않는다.

## 2026-09-21 — 저장소 생성

- 과제 원문을 `planning/T13-ASSIGNMENT.md`에 그대로 옮겼다. 통과 기준은 BRB-C01~C15와
  BRB-C22이고, BRB-C16~C21은 원문에 없다.
- 공개 저장소 `myeongjundev/explainsoc`를 만들었다. 논문 저장소 `explainsoc-research`와
  짝이다. GitHub Pages로 올리면 주소는 https://myeongjundev.github.io/explainsoc/ 가 된다.
- 선행 조건인 마지막 과제 A(12번 사이트)는 2026-09-21에 제출됐다.
- 작업 문서는 `docs/`가 아니라 `planning/`에 둔다. 나중에 `docs/`를 Pages 루트로 쓰면
  작업 문서가 공개 사이트에 섞이기 때문이다.
- 비밀값이 올라가지 않도록 `.gitignore`에서 `.env`를 처음부터 막았다. 예시 파일
  `.env.example`만 올릴 수 있다.

## 2026-09-21 — Claude 의견 정리

- 카드 1 후보와 설계에 넘길 조건을 `planning/CLAUDE-OPINION.md`에 적었다. **결정 문서가 아니다.**
- 요지: 논문의 숫자를 보여 주는 앱이 아니라, 논문이 찾은 "점수 착시" 규칙으로 **사용자가 가져온
  평가 숫자를 점검해 주는 판독기**(후보 A). 낮은 오탐의 함정(B)과 설명 안정성(C)은 A의 판독 항목으로.
- 10번 저장소에 이 과제로 이월된 ExplainSOC 대시보드가 있다(D-003). 결과 열람용이고 FastAPI
  백엔드가 있어 그대로 내기보다 안전선과 문구를 가져오자는 의견을 적었다.
- 설계는 Codex가 맡는다. 작업 요청문은 `planning/CODEX-HANDOFF.md`. 이번 요청은 **설계까지**이고,
  `planning/DESIGN.md`를 쓴 뒤 멈춰 사용자 검토를 받는다. 구현은 승인 뒤다.

## 2026-09-21 — Codex 두 후보에 대한 Claude 답

- Codex가 카드 1 후보로 Evidence Gate와 SplitLens를 내고 Claude 검토를 요청했다
  (`planning/CLAUDE-CARD1-REVIEW.md`). 답은 `planning/CLAUDE-CARD1-ANSWER.md`. **결정 문서가 아니다.**
- 요지: Evidence Gate를 앱으로 두고, SplitLens는 사용자 입력 도구가 아니라 **논문의 실제 분할 그림**
  (원고 IV-3의 학습 공격 9종 대 시험 공격 3종, 겹침 0)으로만 넣는다. "확인됨"은 앱이 보증하는 것처럼
  읽혀 이름을 바꾼다.
- 예시 숫자에서 걸린 것: 미관측 분할 혼동행렬을 원고의 반올림된 오탐률로 역산하면 오탐이 약 75건인데
  `DAY-2-REPORT.md`의 정확한 값은 86건이다. 예시는 그 표에서 가져온다. 또 그 보고서의 "시간" 분할은
  앱에서 원고 용어 "미관측 공격 스트레스 테스트"로 쓴다.
- Codex 요청문에 **과제 원문을 처음부터 끝까지 읽으라**고 적었다. 검토 요청서가 통과 기준 번호만
  기준으로 삼았는데, 원문의 증거 보관 기준("기술의 난이도는 보지 않습니다"), 완주 체크리스트, 카드마다
  막히는 지점과 남길 것이 설계를 가른다. 설계 문서에도 체크리스트와 남길 것을 어떻게 채울지 넣게 했다.

## 2026-09-21 — Codex 페르소나

- `planning/CODEX-PERSONA.md`에 Codex가 어떤 사람으로 일할지를 적었다. 요청문 `CODEX-HANDOFF.md`보다
  먼저 읽게 했다.
- 보안관제센터(성적표를 받아 본 쪽) · 머신러닝 탐지팀(무작위 분할의 함정을 장애로 배운 쪽) · 토스
  (말이 곧 UI) · 지금의 작은 도구 운영, 네 경력이 이 앱이 놓인 자리와 겹치게 잡았다.
- 사용자 요청대로 금지 목록은 넣지 않았다. 지킬 것은 요청문에 있고, 페르소나에는 판단하는 방식과
  이 프로젝트에서 특히 볼 눈만 적었다. Claude 의견 문서는 따를 지시가 아니라 동료 의견으로 읽고, 더 나은
  길이 보이면 반박하라고 적었다.

## 12번 사이트와 잇는 자리

2026-09-22 연결했다. `t12-about/content/approved.json`의 `t13-app`이 `published`이고, 공개 사이트
https://myeongjundev.github.io/about/#work-t13-app 에 13번 카드가 있다(12번 `1dbbbfc`·`ea77b4e`, 제출 뒤
갱신).
