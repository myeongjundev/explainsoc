# ExplainSOC 설계 — 성능표에 없는 질문

> 상태: 사용자 승인 완료 · Claude 구현 인계
> 작성일: 2026-09-21
> 승인일: 2026-09-21
> 범위: 마지막 과제 B의 구현 기준을 고정한다. 변경이 필요하면 구현보다 먼저 이 문서를 갱신한다.

---

## 0. 설계 결론

ExplainSOC는 보안 AI 모델을 합격·불합격으로 판정하는 도구가 아니다. 제품 소개서나 평가표에
적힌 숫자 한두 개를 받은 주니어 보안 담당자가 **그 숫자가 말하지 않는 평가 조건을 찾고,
공급자에게 바로 물을 질문을 준비하는 도구**다.

앱의 서명 장면은 다음 반전이다.

```text
정확도 0.6299 · 오탐률 0.0002
                 ↓
겉으로는 무난해 보이는 성능 주장
                 ↓
공격 220,788건 가운데 탐지 160건
                 ↓
모든 흐름을 정상으로 예측한 기준선 정확도 약 0.6297
                 ↓
“시험에 학습 때 없던 공격이 들어 있었습니까?”
```

정확도 0.6299, FPR 0.0002, 시험 공격 220,788건과 탐지 160건은 논문 V-2의 미관측
공격 XGBoost 결과다. 항상 정상 기준선 약 0.6297은 원고 IV-3의 시험 정상 375,518건을
시험 전량 596,306건으로 나눈 **화면 설명용 파생값**이다. 논문의 새 결과나 일반 임계값으로
부르지 않는다.

기존 Evidence Gate라는 이름은 쓰지 않는다. `Gate`는 앱이 모델을 승인하거나 거절하는 것처럼
읽힌다. 제품 이름은 **ExplainSOC**, 화면 설명 이름은 **성능표에 없는 질문**으로 둔다.

---

## 1. 누구를 돕고 무엇을 남기는가

### 1-1. 사용자가 들고 오는 것

주 사용자는 보안 AI 도입을 처음 맡은 주니어 보안 담당자다. 이 사람은 대개 다음 중 일부만
들고 온다.

- 제품 소개서의 Accuracy, Macro F1, 공격 Recall, FPR 한두 개
- “가장 높은 성능”, “낮은 오탐”, “설명이 안정적” 같은 주장
- 평가 조건은 모르거나 공급자에게 물어보지 않은 상태

혼동행렬과 데이터 분할 문서를 모두 가진 사용자를 정상 경로로 가정하지 않는다. **모름이 가장
중요한 입력**이다.

### 1-2. 사용자가 들고 나가는 것

- 입력한 숫자가 직접 말해 주는 범위
- 빠졌거나 알 수 없는 평가 조건
- 과장 없이 읽기 위한 해석 주의점
- 실제 PoC 자리에서 소리 내어 읽을 수 있는 공급자 질문
- 각 질문이 나온 논문의 절과 원문 근거

앱은 신뢰 점수, 등급, 배포 가능 판정, 모델 추천을 남기지 않는다.

---

## 2. 카드 1 확정 내용

### 2-1. 앱 한 문장 — BRB-C01

> 보안 AI 도입을 처음 맡은 주니어 보안 담당자가 제품 소개서의 성능 숫자 한두 개를
> 받았을 때, 이 앱이 무작위 분할 XGBoost의 Macro F1 0.9979가 미관측 공격 스트레스
> 테스트에서 0.3871로 하락한 논문 결과를 근거로 그 숫자가 말하지 않는 평가 조건과
> 클래스별 오류를 드러내고, 공급자에게 바로 물을 질문을 만들어 준다.

- 돕는 사람: 보안 AI 도입을 처음 맡은 주니어 보안 담당자
- 앱이 하는 일: 빠진 평가 조건과 클래스별 오류를 드러내고 질문으로 바꾼다
- 쓰는 논문 결과: 원고 V-2·VII의 XGBoost Macro F1 `0.9979 → 0.3871`

### 2-2. 사용자가 할 일 세 가지 — BRB-C02

1. 받은 성능 숫자 하나를 적거나 **논문 예시로 60초 검토**를 선택한다.
2. 분할 방식, 미관측 공격 포함 여부, 중복 제거 여부에 `예 / 아니오 / 모름`으로 답한다.
3. 숫자가 말하는 것과 말하지 않는 것을 확인하고 공급자에게 물을 질문을 복사한다.

---

## 3. 제품 원칙

### 3-1. 첫 10초에는 한 가지만 말한다

첫 화면은 “좋아 보이는 보안 AI 숫자도 평가 조건이 빠지면 다르게 읽힐 수 있다”만 말한다.
SHAP, Jaccard, 데이터 감사, 모델 세 개를 첫 화면에 함께 설명하지 않는다.

### 3-2. 모름을 실패로 취급하지 않는다

분할과 중복 제거를 모르는 사용자가 이 앱을 가장 필요로 한다. 빈 답을 오류로 막지 않고
공급자에게 물을 질문으로 바꾼다.

### 3-3. 입력한 값과 확인한 사실을 구분한다

`확인됨`이라는 표현은 쓰지 않는다. 결과 상태는 다음 세 가지다.

- **입력한 근거** — 사용자가 제공한 값이라는 뜻이다
- **확인 필요** — 정보가 없거나 모름이라고 답했다
- **해석 주의** — 숫자는 있지만 단독 해석이 위험하다

어느 상태도 모델 품질 보증을 뜻하지 않는다.

### 3-4. 논문보다 한 발짝 더 말하지 않는다

사용자 입력을 논문 수치와 우열 비교하지 않는다. 임계값을 만들지 않고, 낮음·높음·안전함·
위험함을 자동 판정하지 않는다. 논문의 사례와 확인 방법을 보여 준다.

### 3-5. 입력은 브라우저 밖으로 나가지 않는다

런타임 AI, 백엔드, 분석 스크립트, 광고, 추적 도구를 붙이지 않는다. 입력값은 메모리에만 두고
URL, 쿠키, `localStorage`에 넣지 않는다. 새로 고치면 사라진다.

### 3-6. 차별화는 기능 수가 아니라 근거 연결에서 만든다

질문마다 `왜 묻는가 → 논문 원문 → 이 연구의 한계`를 따라갈 수 있게 한다. 논문 예시의
주장 뒤집기, 항상 정상 기준선, 9종 대 3종 분할 그림이 시각적 중심이다.

---

## 4. 첫 화면 문구 — BRB-C03

### 4-1. 첫 10초 문구

- 제품명: **ExplainSOC**
- 제목: **그 99%, 무엇을 시험한 점수입니까?**
- 도움 한 문장: **보안 AI 도입을 처음 맡은 담당자가 성능표에서 빠진 평가 조건을 찾고,
  공급자에게 물을 질문을 논문 근거와 함께 준비하도록 돕습니다.**
- 주 버튼: **논문 예시로 60초 검토**
- 보조 버튼: **내 성능표 검토**
- 개인정보 안내: **입력한 성능 자료는 이 브라우저 안에서만 계산되며 저장하거나 전송하지
  않습니다.**

### 4-2. 반드시 보이는 논문 제목

첫 화면의 버튼 아래에 다음을 축약하지 않고 표시한다.

> 반영한 논문 · 데이터 분할 방식에 따른 XAI 기반 네트워크 침입 탐지 성능과 설명 안정성 분석

논문 저장소와 PDF 링크는 새 탭으로 열고 외부 링크임을 접근 가능한 이름에 포함한다.

---

## 5. 60초 안의 핵심 경험 — BRB-C03·C05

| 시간 | 사용자가 보는 것 | 제품이 증명하는 것 |
|---:|---|---|
| 0~10초 | 제목, 도움 한 문장, 논문 제목 | 누구를 어떻게 돕는지 안다 |
| 10~15초 | `논문 예시로 60초 검토` 선택 | 설명 없이 시작한다 |
| 15~30초 | 정확도·FPR 카드가 공격 탐지 개수로 뒤집힌다 | 숫자의 착시를 직접 본다 |
| 30~40초 | 학습 공격 9종 대 시험 공격 3종, 겹침 0 | 왜 같은 점수를 확대하면 안 되는지 본다 |
| 40~50초 | `입력한 근거 / 확인 필요 / 해석 주의` | 앱이 보증하지 않음을 안다 |
| 50~60초 | 공급자 질문 목록과 근거, `질문 복사` | 실제 다음 행동을 얻는다 |

이 흐름을 사용성 검증에서 실제로 재고, 60초를 넘으면 고급 설명부터 덜어낸다.

---

## 6. 화면 구조와 흐름 — BRB-C05

별도 라우트가 많은 대시보드 대신 한 페이지 안의 단계형 작업대로 만든다. 뒤로 가도 입력을 잃지
않고, 현재 단계와 전체 세 단계를 항상 표시한다.

### 화면 A. 첫 화면

**목적:** 누구를 어떻게 돕는지 10초 안에 전달한다.

- 4절의 첫 화면 문구
- 논문 결과를 단정이 아니라 질문으로 표현한 짧은 시각 요소
- `논문 예시로 60초 검토`, `내 성능표 검토`
- 브라우저 안에서만 처리한다는 개인정보 안내
- 입력 폼과 긴 연구 한계는 아직 보여 주지 않는다

### 화면 B. 받은 주장 입력

**목적:** 사용자가 가지고 있는 최소 정보부터 받는다.

#### 기본 입력

- 지표 종류: Accuracy / Macro F1 / 공격 Recall / FPR
- 지표 값: 0부터 1까지의 비율
- `지표 하나 더`로 필요한 만큼 추가
- 주장 선택: `가장 좋은 모델이라고 소개했다` — 예 / 아니오 / 모름

모르는 지표를 억지로 채우게 하지 않는다. 혼동행렬을 가진 사용자는 `혼동행렬로 입력`을
선택한다.

#### 혼동행렬 입력

네 개의 작은 칸을 단순히 TN·FP·FN·TP 순으로 늘어놓지 않는다. 2×2 표에 행은 **실제**, 열은
**예측**으로 표시하고 각 칸에 한국어를 함께 쓴다.

| | 예측 정상 | 예측 공격 |
|---|---:|---:|
| 실제 정상 | 정상→정상 (TN) | 정상→공격, 오탐 (FP) |
| 실제 공격 | 공격→정상, 미탐 (FN) | 공격→공격 (TP) |

양성 클래스가 공격임을 표 위에 명시한다. 입력 즉시 Accuracy, 공격 Recall, 정상 Recall,
FPR, Macro F1을 계산하되 계산 결과를 품질 판정으로 바꾸지 않는다.

### 화면 C. 평가 조건 세 질문

**목적:** 논문이 발견한 핵심 조건을 사용자의 말로 묻는다.

1. **시험 자료는 어떻게 나눴습니까?**  
   무작위 / 학습에 없던 공격을 따로 시험 / 다른 방식 / 모름
2. **시험에 학습 때 없던 공격이 들어 있었습니까?**  
   예 / 아니오 / 모름
3. **중복과 학습·시험 사이의 같은 행을 제거했습니까?**  
   예 / 아니오 / 모름

전문용어 설명은 질문 옆 도움말로 두고 답하기 전에 읽도록 강요하지 않는다.

### 화면 D. 주장 뒤집기

**목적:** 숫자를 운영 의미로 바꾸는 핵심 장면을 보여 준다.

논문 예시에서는 다음 순서를 고정한다.

1. `Accuracy 0.6299`, `FPR 0.0002`를 먼저 보여 준다.
2. `공격 220,788건 중 탐지 160건`, `미탐 220,628건`으로 바꿔 보여 준다.
3. `항상 정상 기준선 약 0.6297`과 Accuracy를 같은 축에서 비교한다.
4. “이 비교는 CICIDS2017 미관측 공격 스트레스 테스트의 시험 구성에 한정된다”고 바로 쓴다.

사용자 입력에는 혼동행렬이 있을 때만 개수 번역과 항상 정상 기준선을 계산한다. 지표 하나만
입력했다면 없는 개수를 추정하지 않는다.

### 화면 E. 논문의 실제 분할 그림

**목적:** SplitLens의 시각적 힘을 논문 범위 안에서 사용한다.

- 왼쪽: 월~목 학습 공격 9종
- 오른쪽: 금요일 시험 공격 3종
- 가운데: `공격 유형 겹침 0`
- 아래: “수집 날짜와 공격 유형 변화가 함께 바뀌었으므로 순수 시간 일반화가 아니라 미관측
  공격 스트레스 테스트다.”

사용자가 공격 유형을 추가하거나 임의 분할을 판정하는 기능은 넣지 않는다.

### 화면 F. 결과와 질문

**목적:** 분석이 아니라 다음 행동을 준다.

- 입력한 근거
- 확인 필요
- 해석 주의
- 공급자 질문 목록
- 각 항목의 `논문 근거 보기`
- `질문 복사`
- `입력 수정`, `처음부터`

질문 복사는 사용자가 입력한 비밀 숫자를 포함하지 않고 질문 문장만 복사한다. 복사 성공은
텍스트와 `aria-live`로 알린다.

### 화면 G. 연구 범위와 근거

**목적:** 긴 설명을 핵심 흐름 밖에서 확인하게 한다.

- 논문 수치와 원문
- CICIDS2017 한 데이터셋에 한정된다는 점
- SHAP과 실제 SOC 평가에 대한 경계
- 논문 저장소·PDF
- “이 앱이 하는 말 / 하지 않는 말”

---

## 7. 판독 규칙 — BRB-C04

규칙은 순수 TypeScript 함수와 데이터 표로 구현한다. 규칙 ID, 입력 조건, 결과 문구, 질문,
논문 근거를 한 객체에 둔다. 임계값 기반 등급은 만들지 않는다.

| ID | 입력 조건 | 화면 안내 | 공급자 질문 | 논문 근거 |
|---|---|---|---|---|
| R01 | 분할이 무작위 | 해석 주의: 관측한 공격이 섞인 시험의 높은 점수를 미관측 공격 성능으로 확대할 수 없다 | 학습에 없던 공격만 따로 둔 시험 결과도 있습니까? | VII·S1, V-2 |
| R02 | 분할이 모름 | 확인 필요: 점수를 만든 시험 조건을 알 수 없다 | 학습과 시험 자료를 어떤 기준으로 나눴습니까? | VII·S1, IV-3 |
| R03 | Accuracy는 있으나 공격 Recall과 혼동행렬이 없음 | 해석 주의: 전체 정확도만으로 공격 미탐을 알 수 없다 | 공격 Recall과 TN·FP·FN·TP를 제공할 수 있습니까? | VI-3·S3 |
| R04 | FPR은 있으나 공격 Recall이 없음 | 해석 주의: 낮아 보이는 오탐률만으로 공격 탐지 능력을 알 수 없다 | 같은 시험에서 공격 Recall은 얼마입니까? | V-2·S5 |
| R05 | `가장 좋은 모델` 주장이고 한 분할 결과만 있음 | 확인 필요: 분할에 따라 모델 순위가 달라질 수 있다 | 다른 분할에서도 같은 모델이 가장 높았습니까? | V-2·S6 |
| R06 | 중복 제거가 아니오 | 해석 주의: 중복을 처리하지 않은 평가는 낙관적일 수 있다 | 완전 중복과 학습·시험 중복을 제거한 결과가 있습니까? | V-1, IV-3 |
| R07 | 중복 제거가 모름 | 확인 필요: 중복 통제 여부를 알 수 없다 | 중복과 학습·시험 사이 같은 행을 검사했습니까? | V-1, IV-3 |
| R08 | 미관측 공격 포함이 아니오 | 입력한 근거: 이 시험은 학습에서 보지 못한 공격을 직접 검증하지 않았다 | 미관측 공격만 별도로 둔 스트레스 테스트가 있습니까? | VII·S1 |
| R09 | 미관측 공격 포함이 모름 | 확인 필요: 새 공격을 시험했는지 알 수 없다 | 시험에 학습 때 없던 공격 유형이 들어 있었습니까? | IV-3, VII·S1 |
| R10 | FPR과 공격 Recall이 모두 있음 | 입력한 근거: 두 값을 나란히 제시한다. 높고 낮음을 자동 판정하지 않는다 | 필요하면 혼동행렬 원수치를 제공할 수 있습니까? | VI-3·S3 |

`R04`는 임의의 낮은 FPR 기준을 만들지 않는다. FPR 값이 있다는 이유만으로 낮다고 판정하지
않고, 공격 Recall이 빠졌다는 사실만 지적한다. 논문 예시에서만 원고의 표현에 따라 FPR
0.0002를 “매우 낮은 FPR”이라고 쓴다.

설명 안정성 판독(S2·S4)은 첫 판에 넣지 않는다. 사용자가 SHAP 정보를 가지고 온다는 가정이
카드 1의 주 사용자와 맞지 않고 첫 흐름을 키우기 때문이다. 구조는 이후 R11로 추가할 수 있다.

---

## 8. 앱이 사용하는 논문 결과와 원문 — BRB-C04

화면에 쓰는 수치와 규칙마다 아래 근거 ID를 연결한다. 문구를 바꿀 때 원문보다 강해지지 않았는지
검토한다.

### P01. 분할 정의와 공격 유형 — IV-3

> 날짜 기반 분할에서 월~목 학습 공격은 DoS Hulk, DoS GoldenEye, FTP-Patator,
> SSH-Patator, DoS slowloris, DoS Slowhttptest, Web Attack, Infiltration,
> Heartbleed다. 금요일 시험 공격은 DDoS, PortScan, Bot이며 두 집합은 겹치지 않는다.

> 따라서 이 분할은 동일 공격의 미래 성능을 측정하는 순수 시간 분할이 아니라, 수집 날짜와
> 공격 유형 변화가 결합된 미관측 공격 스트레스 테스트다.

사용처: 화면 E, R01·R02·R08·R09, 분할 이름 전체.

### P02. 평가 지표를 함께 본 이유 — IV-4

> Accuracy와 함께 클래스 불균형의 영향을 줄이기 위해 Macro F1을 주 지표로 사용하였다.
> 운영 관점의 실패를 확인하기 위해 공격 Recall, 정상 Recall, 오탐률(FPR), 혼동행렬도 함께
> 기록하였다. FPR은 `FP/(FP+TN)`으로 계산하였다.

사용처: 혼동행렬 계산, 지표 설명, R03·R04·R10.

### P03. 무작위와 미관측 공격 XGBoost — V-2

| 조건 | Accuracy | Macro F1 | 공격 Recall | FPR |
|---|---:|---:|---:|---:|
| 무작위 XGBoost | 0.9988 | 0.9979 | 0.9974 | 0.0009 |
| 미관측 공격 XGBoost | 0.6299 | 0.3871 | 0.0007 | 0.0002 |

> 무작위 분할에서는 두 트리 모델의 Macro F1이 0.997을 넘었다. 특히 XGBoost는 시험
> 공격 85,175건 중 84,955건을 탐지하고 정상 419,296건 중 362건을 오탐하였다. 반면
> 미관측 공격 분할에서 XGBoost는 공격 220,788건 중 160건만 탐지하였다. 매우 낮은
> FPR 0.0002는 좋은 결과처럼 보이지만, 이는 모델이 거의 모든 흐름을 정상으로 예측한
> 결과이므로 공격 Recall과 함께 읽어야 한다.

사용처: 첫 화면 예고, 화면 D, R01·R03·R04, 60초 예시.

### P04. 모델 순위 변화 — V-2

> 또한 분할 방식에 따라 모델 순위도 달라졌다.

사용처: R05. Logistic Regression의 상대 우위를 “단순 모델이 항상 더 낫다”로 바꾸지 않는다.

### P05. 중복 감사 — V-1·IV-3

> 완전 중복은 308,381건으로 전체의 10.894%였으며, 제거 후 BENIGN 2,096,484건과
> ATTACK 425,878건이 남았다.

> 월~목에도 나타난 금요일 고유 행 19,588건은 시험에서 추가 제거하였다. 최종 학습·시험
> `row_id` 교집합은 0건이다.

사용처: R06·R07. 10.894%를 모든 데이터셋에 적용하는 기준으로 쓰지 않는다.

### P06. 운영적 확인 방법 — VI-3

> 운영자는 전체 정확도만 보지 않고 공격 Recall과 혼동행렬을 함께 확인해야 한다.

사용처: R03·R04·R10, 질문 목록의 우선순위.

### P07. 결론 — VII

> 이 결과는 한 가지 무작위 분할의 높은 성능만으로 미관측 공격 탐지 능력을 판단해서는
> 안 됨을 보여 주며

사용처: 카드 1, 첫 화면, R01·R02·R08·R09.

### P08. 설명 안정성의 경계 — VI-2·VII

> 따라서 안정적인 설명은 정확하거나 유용한 탐지의 보증이 아니다.

사용처: 연구 범위 화면. 첫 판의 사용자 판독 규칙에는 넣지 않는다.

### P09. 정확한 예시 혼동행렬 — DAY-2-REPORT

원고의 반올림 비율에서 역산하지 않는다. 예시 원수치는 제출된 연구의
`docs/DAY-2-REPORT.md`에서 가져온다. 배열 순서는 `[[TN, FP], [FN, TP]]`다.

| 조건 | TN | FP | FN | TP |
|---|---:|---:|---:|---:|
| 무작위 XGBoost | 418,934 | 362 | 220 | 84,955 |
| 미관측 공격 XGBoost | 375,432 | 86 | 220,628 | 160 |

DAY-2 보고서의 `시간`이라는 이름은 데이터 키에서만 남을 수 있다. 화면에서는 모두 원고 용어인
`미관측 공격 스트레스 테스트`로 바꾼다.

### P10. 항상 정상 기준선 — IV-3에서 파생

- 시험 정상: 375,518
- 시험 전량: 596,306
- 계산: `375,518 / 596,306 ≈ 0.6297`
- 미관측 공격 XGBoost Accuracy: 0.6299

화면 문구:

> 같은 시험 자료에서 모든 흐름을 정상으로만 예측한 경우의 정확도는 약 0.6297입니다.
> XGBoost의 0.6299와 거의 같아 보이는 이유를 이해하기 위한 비교이며, 다른 데이터셋의
> 기준이나 합격선이 아닙니다.

이 값은 계산식과 입력 원수를 함께 표시한다. 소수 넷째 자리 반올림값만 단독으로 저장하지 않는다.

---

## 9. 혼동행렬 계산

양성 클래스는 ATTACK이다. 입력 원수로 다음을 계산한다.

```text
total = TN + FP + FN + TP
Accuracy = (TN + TP) / total
공격 Recall = TP / (TP + FN)
정상 Recall = TN / (TN + FP)
FPR = FP / (FP + TN)
공격 Precision = TP / (TP + FP)
정상 Precision = TN / (TN + FN)
공격 F1 = 2 × 공격 Precision × 공격 Recall / (공격 Precision + 공격 Recall)
정상 F1 = 2 × 정상 Precision × 정상 Recall / (정상 Precision + 정상 Recall)
Macro F1 = (공격 F1 + 정상 F1) / 2
항상 정상 기준선 Accuracy = (TN + FP) / total
```

분모가 0이면 0으로 만들지 않고 `계산할 수 없음`을 반환한다. 계산 결과는 화면에서 최대 소수
넷째 자리까지 보이되 테스트는 반올림 전 값을 검증한다.

---

## 10. 입력 검증과 실패 처리 — BRB-C05

오류는 앱 전체를 막지 않고 해당 칸 가까이에 무엇이 잘못됐고 어떻게 고칠지 표시한다.

| 입력 | 잘못된 경우 | 처리와 문구 |
|---|---|---|
| 선택 지표 | 선택하지 않음 | 숫자 칸을 열지 않고 “먼저 지표 이름을 골라 주세요” |
| 비율 | 빈 칸 | 허용한다. `정보 없음`으로 질문을 만든다 |
| 비율 | 숫자가 아닌 글자 | 계산에서 제외하고 “0부터 1 사이의 숫자로 적어 주세요” |
| 비율 | 음수 | 계산에서 제외하고 “비율은 0보다 작을 수 없습니다” |
| 비율 | 1 초과 | 계산에서 제외하고 “비율은 0부터 1 사이로 적어 주세요. 99%는 0.99입니다” |
| 비율 | NaN·Infinity | 계산에서 제외하고 일반 숫자 입력을 안내한다 |
| 혼동행렬 | 빈 칸 | 해당 값만 `정보 없음`; 네 칸 계산은 시작하지 않는다 |
| 혼동행렬 | 글자·소수·음수 | “0 이상의 정수로 적어 주세요” |
| 혼동행렬 | 안전 정수 초과 | “지원 범위보다 큰 값입니다” |
| 혼동행렬 | 네 칸의 합이 0 | “평가한 항목이 없어 지표를 계산할 수 없습니다” |
| 혼동행렬 | `TP+FN=0` | 공격 Recall을 `계산할 수 없음`; 공격 표본이 없다고 설명한다 |
| 혼동행렬 | `FP+TN=0` | FPR·정상 Recall을 `계산할 수 없음`; 정상 표본이 없다고 설명한다 |
| 평가 조건 | 미선택 | `모름`과 동일하게 질문을 만들되, 제출 전 선택을 요구하지 않는다 |
| 붙여넣기 | 쉼표 포함 숫자 | 쉼표와 양끝 공백을 안전하게 제거해 정수로 읽는다 |
| 텍스트 | HTML·스크립트 문자열 | React 텍스트로만 렌더링하고 HTML 삽입 API를 쓰지 않는다 |

오류가 있어도 나머지 유효한 입력으로 질문 목록을 만든다. 잘못된 값을 0으로 바꾸지 않는다.

---

## 11. 예시 데이터 — BRB-C05·C06

### 예시 A. 60초 검토 기본값

미관측 공격 XGBoost의 정확한 값을 사용한다.

```text
표시 주장: Accuracy 0.6299, FPR 0.0002
TN 375432 · FP 86 · FN 220628 · TP 160
분할: 미관측 공격 스트레스 테스트
미관측 공격 포함: 예
중복 제거: 예
```

### 예시 B. 높은 무작위 분할 점수

무작위 XGBoost의 정확한 값을 사용한다.

```text
Accuracy 0.9988 · Macro F1 0.9979 · 공격 Recall 0.9974 · FPR 0.0009
TN 418934 · FP 362 · FN 220 · TP 84955
분할: 계층화 무작위
미관측 공격 포함: 아니오
중복 제거: 예
```

두 예시는 공개 데이터셋을 이용한 논문의 집계 결과다. 원자료, IP, 사용자 이름, 실제 조직 자료를
포함하지 않는다. 임의의 회사나 제품 이름도 넣지 않는다.

---

## 12. 시각·상호작용 설계

### 12-1. 시각 언어

보안 관제 대시보드를 그대로 복제하지 않는다. 어두운 배경에 표를 빽빽하게 놓는 대신, 한 장의
성능 주장 문서가 검토되며 층이 벗겨지는 모양을 사용한다.

- `주장`: 중립 표면
- `입력한 근거`: 파랑 + 문서 아이콘
- `확인 필요`: 호박색 + 물음표 아이콘
- `해석 주의`: 자홍/적색에 의존하지 않는 빗금 + 주의 아이콘
- 모든 상태는 색, 아이콘, 텍스트를 함께 쓴다

숫자 축은 0부터 시작한다. `0.9974`와 `0.0007`을 같은 축에 놓을 때 작은 값에 `220,788건 중
160건`을 함께 써 0으로 오해하지 않게 한다. 작은 차이를 확대하는 잘린 축을 쓰지 않는다.

### 12-2. 반응형

- 375px부터 가로 스크롤 없이 사용한다
- 2×2 혼동행렬은 모바일에서도 2×2를 유지하되 설명은 짧게 접는다
- 질문 복사와 다음 단계 버튼은 엄지로 누를 수 있는 크기를 확보한다
- 데스크톱에서는 입력과 결과를 나란히, 모바일에서는 입력 다음 결과 순서로 둔다
- 고정 하단 버튼이 오류 문구나 마지막 질문을 가리지 않게 안전 영역과 본문 여백을 둔다

### 12-3. 접근성

- 모든 입력에 보이는 `<label>`을 둔다
- 단계 이동, 오류 요약, 복사 결과는 스크린 리더로 전달한다
- 상태를 색으로만 구분하지 않는다
- 키보드만으로 예시 실행, 입력, 결과 확인, 질문 복사가 가능해야 한다
- 모션 감소 설정에서는 카드 뒤집기를 즉시 전환으로 바꾼다
- 수치 표와 시각화에 같은 내용을 담은 텍스트 대안을 둔다
- 포커스가 보이고 모달을 쓰지 않는다

---

## 13. 기술 선택과 구조

### 13-1. 선택

- React 19
- TypeScript
- Vite
- CSS와 접근 가능한 HTML
- 단순 SVG 또는 CSS 시각화
- Vitest 기반 단위 테스트
- Playwright 기반 핵심 브라우저 흐름 검증

넘겨받은 대시보드와 같은 프론트 기술을 써 검증된 자산을 읽기 쉽지만, 코드는 T13 저장소에
새로 구성한다. 차트 라이브러리, 상태 관리 라이브러리, 런타임 AI SDK는 넣지 않는다.

### 13-2. 폴더 초안

```text
src/
  app/
    App.tsx
  components/
    Hero.tsx
    MetricInput.tsx
    ConfusionMatrixInput.tsx
    EvaluationQuestions.tsx
    ClaimReveal.tsx
    SplitEvidenceFigure.tsx
    FindingList.tsx
    EvidenceDrawer.tsx
  domain/
    metrics.ts
    reviewRules.ts
    validation.ts
    types.ts
  data/
    paperEvidence.ts
    examples.ts
  styles/
tests/
  unit/
  e2e/
evidence/
release/
docs/
```

### 13-3. 데이터 구조

`paperEvidence.ts`에 근거 ID, 절, 원문, 수치, 화면 사용처를 둔다. `reviewRules.ts`는 근거
ID만 참조한다. 화면 컴포넌트에 논문 숫자와 판독 문구를 흩어 쓰지 않는다.

규칙 하나에는 최소 다음이 들어간다.

```text
id · 조건 함수 · 상태 · 제목 · 설명 · 질문 · evidenceIds
```

판독 순서는 위험 점수가 아니라 사용자 행동 순서로 고정한다. `확인 필요 → 해석 주의 → 입력한
근거` 순서로 보여 주고 자동 점수는 계산하지 않는다.

---

## 14. 비밀값·개인정보·보안 정책 — BRB-C06·C07·C12

- 앱에 런타임 AI 서비스를 붙이지 않는다. 과제의 “AI와 함께 바이브코딩”은 제작 과정이며 앱이
  API를 호출해야 한다는 뜻이 아니다.
- API 키와 환경 변수 자체가 필요 없다. README에는 **필요한 환경 변수 없음**이라고 적는다.
- `.env`, `.env.*`는 현재 `.gitignore` 정책을 유지한다.
- 사용자 입력은 브라우저 메모리에만 둔다.
- 사용자 입력을 URL, 로그, 분석 도구, 외부 요청, 저장소에 넣지 않는다.
- 외부 폰트와 외부 이미지 요청을 사용하지 않는다.
- `dangerouslySetInnerHTML`을 사용하지 않는다.
- 상태 공유 링크와 파일 업로드는 첫 판에 만들지 않는다.
- 예시 데이터는 CICIDS2017 집계 숫자뿐이며 본인 외 실명·연락처를 넣지 않는다.
- CI에서 비밀값 패턴, 외부 호스트, 개인정보 후보를 검사한다.

---

## 15. 넘겨받은 ExplainSOC 대시보드 처리

10번 대시보드는 연구 결과를 열람하는 네 화면이며 FastAPI 고정 산출물 API가 필요하다. 이번 앱의
목적은 사용자의 성능 주장을 질문으로 바꾸는 것이므로 그대로 이식하지 않는다.

### 가져올 것

- React 19·Vite·TypeScript라는 검증된 기술 방향
- `SHAP은 인과가 아니라 모델 내부 판단 근거`라는 안전선
- `시간 분할`을 미래 성능으로 부르지 않는 안전선
- 모델·분할 이름을 일관되게 표시한 경험
- 오탐·미탐을 분리해 읽는 시각 원칙

### 새로 만들 것

- 숫자 한두 개부터 시작하는 입력 흐름
- 예/아니오/모름 평가 질문
- 판독 규칙과 질문 생성
- 혼동행렬 실시간 계산
- 주장 뒤집기와 항상 정상 기준선
- 논문의 9종 대 3종 정적 분할 그림
- 논문 원문까지 이어지는 근거 구조

### 버릴 것

- FastAPI 백엔드와 `/api` 의존성
- Overview, Model Comparison, Attack Analysis, Event Detail 네 화면 구조
- PCAP 업로드, Kali 데모, 모델 추론
- Google Fonts 같은 외부 런타임 요청

### 이후 고려

첫 판이 승인되고 핵심 흐름을 해치지 않을 때만 기존 대시보드의 결과 화면을 `연구 근거 자세히
보기`로 정적 변환할 수 있다. 첫 판 필수 범위는 아니다.

---

## 16. 배포와 실행 묶음 — BRB-C08·C09·C15·C22

### 공개 URL

- GitHub Pages
- 목표 주소: `https://myeongjundev.github.io/explainsoc/`
- Vite base: `/explainsoc/`
- 로그인, 계정, OAuth, 초대, 비밀번호, CAPTCHA 없음
- 새 시크릿 창에서 첫 화면과 전체 60초 흐름을 확인한다

### 실행 묶음

공개 URL을 주 제출물로 두고, 비밀번호 없는 ZIP도 함께 낸다. ZIP에는 소스, `package-lock.json`,
README, 예시 데이터, 테스트와 라이선스 정보를 넣고 `node_modules`, Git 기록, 로그, 환경 파일,
작업자 PC 경로를 넣지 않는다.

README 실행 절차는 새 임시 폴더에서 다음만 요구한다.

```text
npm ci
npm run build
npm run preview
```

릴리스 검증은 ZIP을 새 임시 폴더에 풀고 README 순서대로 실행해 HTTP 응답과 첫 화면을 확인한다.
공개 URL이 C22를 이미 충족하지만, ZIP은 C09와 재현성을 함께 증명한다.

---

## 17. 테스트와 검증 계획

### 17-1. 단위 테스트

- 혼동행렬의 각 지표 계산
- P09의 두 XGBoost 혼동행렬이 원고 V-2 반올림 지표와 맞는지
- 항상 정상 기준선 계산
- 0 분모 처리
- R01~R10 각각 조건 하나당 테스트 하나 이상
- 모름만 입력해도 질문 목록이 비지 않는지
- 임의 임계값이나 합격 판정이 결과에 없는지
- 화면용 근거 ID가 실제 근거 데이터에 모두 존재하는지
- `시간 분할`, `미래 성능`, `확인됨` 금지 문구 검사

### 17-2. 컴포넌트 테스트

- 보이는 label과 오류 연결
- 잘못된 입력이 다른 유효 입력 결과를 막지 않는지
- 논문 예시 버튼이 정확한 P09 원수치를 넣는지
- 질문 복사가 질문만 포함하고 사용자 숫자를 포함하지 않는지
- 예/아니오/모름 키보드 조작

### 17-3. 브라우저 검증

- 60초 예시 전체 흐름
- 내 성능표에서 지표 하나만 입력한 흐름
- 모든 조건이 모름인 흐름
- 혼동행렬 유효·음수·문자·합 0 흐름
- 새로 고침 뒤 입력이 저장되지 않는지
- 네트워크 요청이 정적 자산 외에 없는지
- 375×812, 768×1024, 1440×900 화면
- 키보드 전체 이동, 포커스 표시, 스크린 리더 이름
- 라이트·다크를 모두 만들 경우 두 테마 대비
- 모션 감소 설정

### 17-4. 보안·릴리스 검증

- 비밀값과 개인정보 후보 문자열 검사
- 외부 호스트 요청 검사
- `dangerouslySetInnerHTML`과 동적 코드 실행 검사
- 의존성 감사
- 프로덕션 빌드
- GitHub Pages 새 시크릿 창
- ZIP 허용 목록과 새 임시 폴더 실행

---

## 18. 통과 기준별 증거 계획

| 기준 | 구현·검증 방법 | 남길 증거 |
|---|---|---|
| BRB-C01 | 2-1의 문장을 README와 첫 화면 도움 문장에 연결한다 | `evidence/card-1.md` |
| BRB-C02 | 세 행동을 단계 표시와 README 확인 방법에 그대로 쓴다 | 세 행동 E2E 결과 |
| BRB-C03 | 논문 제목과 도움 한 문장을 첫 화면 DOM에서 검사한다 | 첫 화면 스크린샷·자동 검사 |
| BRB-C04 | R01~R10과 P01~P10을 코드로 연결한다 | 규칙 테스트·근거 표 |
| BRB-C05 | 60초 예시, 모름 경로, 잘못된 입력을 검증한다 | E2E 로그·오류 화면 |
| BRB-C06 | P09 집계 예시만 포함하고 데이터 목록을 검사한다 | 예시 데이터 명세 |
| BRB-C07 | 런타임 비밀값이 없고 `.env`가 제외되는지 검사한다 | 비밀값 검사 결과·README |
| BRB-C08 | Pages 공개 URL과 ZIP 둘 다 준비한다 | 공개 URL·릴리스 ZIP |
| BRB-C09 | 새 임시 폴더에서 README대로 ZIP을 실행한다 | clean-room 실행 로그 |
| BRB-C10 | 앱 공개 후 T12 `t13-app` 자리에 링크와 한 문장을 넣는다 | 대표작 화면·커밋 |
| BRB-C11 | 처음 보는 동료 한 명이 설명 없이 써 보고 익명 한 줄을 준다 | 이름 없는 감상과 날짜 |
| BRB-C12 | 본인 외 실명·연락처·비밀값을 전체 제출물에서 검사한다 | 릴리스 검사 결과 |
| BRB-C13 | URL, 세 행동, 통과 결과를 짧은 확인 방법에 적는다 | 제출 확인 문서 |
| BRB-C14 | AI가 한 일, 사용자가 판단한 일, 따르지 않은 제안을 나눈다 | 사용자가 직접 쓴 제출문 |
| BRB-C15 | 모든 제출 URL을 새 시크릿 창에서 로그인 없이 연다 | URL별 확인 기록 |
| BRB-C22 | 공개 URL과 비밀번호 없는 ZIP 중 하나 이상, 계획상 둘 다 낸다 | 제출 항목 목록 |

C16~C21은 원문에 없으므로 만들거나 검사하지 않는다.

---

## 19. 완주 체크리스트 여섯 줄을 지우는 방법

| 원문 체크리스트 | 지우는 조건 |
|---|---|
| 논문에서 앱이 될 문장 하나와 사용자가 할 일 세 가지를 정했다 | 2절을 사용자 승인하고 `evidence/card-1.md`에 남긴다 |
| 첫 화면에 논문 제목과 누구를 어떻게 돕는가 한 문장을 두었다 | DOM 검사, 데스크톱·모바일 스크린샷으로 확인한다 |
| 앱이 논문의 결과를 실제로 쓰고 잘못된 입력에도 멈추지 않는다 | R01~R10 단위 테스트와 오류 E2E를 통과한다 |
| 실제 개인정보와 비밀값을 뺐다 | 데이터 목록과 비밀값 검사를 통과한다 |
| 새 시크릿 창 또는 새 임시 폴더에서 열린다 | Pages 시크릿 창과 ZIP clean-room을 둘 다 확인한다 |
| 12번 사이트 대표작 자리에 앱을 연결했다 | 공개 뒤 T12 링크와 한 문장을 확인한다 |

---

## 20. 카드마다 남길 것

증거는 마지막 날 기억으로 작성하지 않고 카드가 끝날 때 바로 남긴다.

### 카드 1

- `evidence/card-1.md`
- 확정한 앱 한 문장
- 사용자 행동 세 가지
- 사용자 승인 날짜

### 카드 2

- `evidence/card-2.md`
- 첫 화면 데스크톱·모바일 스크린샷
- 세 행동을 직접 수행한 결과
- 잘못된 입력 목록과 실제 화면
- 60초 측정 결과

### 카드 3

- `evidence/card-3.md`
- 예시 데이터의 출처와 필드 목록
- 개인정보·비밀값 검사 결과
- README의 환경 변수 없음 문구

### 카드 4

- `evidence/card-4.md`
- 공개 URL
- 새 시크릿 창 확인 환경과 결과
- ZIP 파일명·해시
- 새 임시 폴더 실행 명령과 결과

### 카드 5

- `evidence/card-5.md`
- T12 대표작 자리 스크린샷과 링크
- 동료의 이름 없는 한 줄 감상
- 짧은 확인 방법
- 제출 URL·ZIP 목록
- AI에게 맡긴 일과 따르지 않은 제안 초안

`내가 직접 판단한 일`은 사용자가 직접 쓴다. 앱이나 Codex가 대신 작성하지 않는다.

---

## 21. 구현 범위

### 첫 판에 꼭 넣는다

- 첫 10초 문구와 논문 제목
- 논문 예시 60초 경로
- 최소 지표 입력과 혼동행렬 입력
- 평가 조건 세 질문
- R01~R10 판독
- 주장 뒤집기와 항상 정상 기준선
- 논문의 9종 대 3종 정적 분할 그림
- 질문 목록과 복사
- P01~P10 근거 보기
- 잘못된 입력 처리
- 모바일·접근성
- 규칙·계산 단위 테스트와 핵심 E2E
- 개인정보·비밀값·외부 요청 검사
- Pages와 ZIP

### 첫 판 뒤에만 고려한다

- SHAP 설명 안정성 판독 R11
- 인쇄용 한 장 검토표
- 기존 대시보드 정적 근거 화면
- 여러 모델 비교 입력

### 만들지 않는다

- 사용자 공격 유형 배치형 SplitLens
- PCAP·CSV 업로드
- 라이브 모델 추론
- 런타임 AI 답변
- 자동 신뢰 점수와 합격 판정
- 계정, 로그인, 공유 링크, 서버 저장

---

## 22. 사용자가 승인한 구현 결정

사용자는 2026-09-21 추천 방향으로 진행하도록 승인했다. 구현에서는 다음 결정을 다시 열지 않는다.

1. 2절의 카드 1 문장과 사용자 행동 세 가지를 유지한다.
2. 첫 화면 제목은 `그 99%, 무엇을 시험한 점수입니까?`로 한다.
3. 제품 설명 이름은 `성능표에 없는 질문`으로 한다.
4. 첫 판에 9종 대 3종 정적 분할 그림을 포함한다.
5. 공개 URL과 실행 ZIP을 모두 제출한다.

구현 중 새 기능이 떠나도 이 다섯 결정을 바꾸지 않는다. 바꿔야 할 근거가 생기면 구현을 넓히기
전에 설계 문서를 먼저 갱신하고 사용자에게 알린다.
