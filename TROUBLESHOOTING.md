# 트러블슈팅 기록 — ExplainSOC

> ExplainSOC(성능표에 없는 질문)를 만들며 실제로 부딪힌 문제와 해결을 모았다. 기간은 2026-09-21~22다.
> 모든 항목은 이 저장소의 커밋·테스트·증거 문서로 되짚을 수 있다.
> 기록한 사람: Claude(구현 담당) · 2026-09-22

## 한눈에 보기

| # | 문제 | 원인 | 해결 | 확인 |
|---|---|---|---|---|
| 1 | 브라우저 테스트에 앱이 하지 않은 요청과 오류가 섞였다 | 개발 PC의 광고 차단 프로그램이 Chrome·Edge의 HTTPS를 풀어 모든 페이지에 스크립트를 끼워 넣었다 | 테스트는 빌드 결과물을 네트워크 없이 읽고, 공개 주소는 테스트 도구가 받아 넘긴다 | 이 PC·CI·공개 주소에서 브라우저 테스트 20개 |
| 2 | 반올림된 오탐률에서 거꾸로 계산하면 오탐이 약 75건 | 원고의 FPR 0.0002는 반올림 값이다(정확한 값은 86건) | 제출 연구 보고서의 정확한 혼동행렬을 쓰고, 원고 지표 8개를 재현하는 테스트를 둔다 | 단위 테스트 |
| 3 | 정확도와 기준선의 차이를 "0.0002"로 적었다 | 반올림한 두 값의 차였다 | 실제 차이 596,306건 가운데 74건(TP−FP)으로 고쳐 테스트로 고정 | 단위 테스트 |
| 4 | 재현한다고 말할 수 없는 ZIP | 검사기가 Git에 의존했고, 줄 끝이 PC마다 달랐고, 코드와 ZIP 순서가 꼬였다 | 허용 목록 ZIP 스크립트, Git 없는 검사, "코드 커밋 → ZIP → 해시 기록" 순서 | 두 번 만들어 같은 SHA-256, 새 임시 폴더 실행 |
| 5 | 제출물에 다른 사람 실명과 검사기 시험값이 샜다 | 검사기를 시험한 값을 문서·주석에 그대로 적었고, 데이터셋 인용에 저자 이름이 있었다 | 검사기는 그대로 두고 문서·인용·주석을 고쳤다 | 풀어 본 ZIP·공개 배포본 검색 0건 |
| 6 | 375px 화면 가로 넘침, 그래프 축 어긋남, 스크린 리더가 붙여 읽는 링크 이름 | 그리드 칸의 기본 최소 너비, 글자 길이에 따라 변하는 칸, 빠진 공백 | `min-width: 0`, 공유 CSS 변수, 명시적 공백 | 3개 폭 E2E, axe WCAG 2.2 AA |
| 7 | 배포본에서 React 라이선스 고지가 사라졌다 | 번들러가 주석을 지웠고, Pages 업로드는 점으로 시작하는 파일을 뺀다 | Vite `build.license`를 배포본 맨 위에 둔다 | 공개 주소에서 200 |
| 8 | README의 Node 버전이 틀렸다 | 번들러 요구만 보고 테스트 도구 요구를 보지 않았다 | engines를 vitest·jsdom 요구 범위로 | CI Node 24 |
| 9 | 포트폴리오 카드에서 핵심 문장이 잘렸다 | 카드 그림 칸은 세로인데 가로 화면을 넣었다 | 핵심 칸만 세로 3:4로 다시 찍었다 | 세 곳 스크린샷 |
| 10 | 다른 PC에서 올린 커밋을 모르고 편집을 시작했다 | 집·학원 두 PC에서 같은 저장소를 쓴다 | 편집 전에 `git fetch`와 `git status -sb` | 작업 규칙 |

---

## 1. 브라우저 테스트가 앱이 하지 않은 일을 잡았다

**상황.** 로컬 브라우저 테스트의 "앱 밖으로 나가는 요청 0건"과 "콘솔 오류 0건" 검사가 실패했다. 앱 코드에는
요청을 보내는 곳이 없고, 배포본에는 `connect-src 'none'` 콘텐츠 보안 정책까지 걸려 있었다.

**원인 찾기.**

1. 같은 페이지를 `curl`로 받은 HTML과 브라우저가 받은 HTML을 비교했다. 브라우저 쪽에만 맨 앞에 237KB 인라인
   스크립트와 외부 스크립트가 붙어 있었다. `localhost`도 마찬가지였다.
2. 콘솔 오류의 정체는 끼워 넣어진 스크립트가 밖으로 보내려던 요청이 앱의 콘텐츠 보안 정책에 막힌 기록이었다.
3. 공개한 뒤 HTTPS에서도 확인했다. 설치된 Chrome이 본 인증서 발급자는 example.com, github.com, github.io 모두
   광고 차단 프로그램의 자체 루트 인증서였다. 같은 PC의 Node가 본 발급자는 원래 발급자(Cloudflare, Sectigo,
   Let's Encrypt)였다. 그 프로그램의 설정에는 가로챌 대상으로 설치된 Chrome과 Edge의 경로 둘만 있었다.

개발 PC에 깔린 상용 광고 차단 프로그램이 원인이었다. 커널 드라이버와 자체 루트 인증서로 두 브라우저의 HTTPS를
풀고, 모든 페이지에 스크립트를 끼워 넣고 있었다. 같은 PC에서도 Node와 `curl`이 받은 내용은 깨끗했던 이유다.

**해결.**

- 테스트 fixture([`tests/e2e/fixtures.ts`](tests/e2e/fixtures.ts))가 모든 요청을 가로챈다. 앱 출처의 요청은
  `dist`에서 바로 돌려주고, 그 밖의 요청은 막고 목록에 남긴다. 네트워크를 거치지 않으니 끼워 넣을 틈이 없고,
  "앱이 스스로 보낸 요청"만 검사하게 된다.
- 공개 주소 검사(`LIVE_URL=… npx playwright test`)는 `route.fetch()`로 테스트 도구가 공개 서버에서 받아 페이지에
  넘긴다. 페이지가 받는 바이트는 GitHub Pages가 보낸 그대로다.
- 시스템에 깔린 프로그램은 건드리지 않았다. 진단 결과와 제거 방법은 PC 주인에게 전했다.

**결과.** 같은 브라우저 테스트 20개가 이 PC, CI(Ubuntu·headless Chromium), 공개 주소에서 모두 통과한다.
([`evidence/card-2.md`](evidence/card-2.md), [`evidence/card-4.md`](evidence/card-4.md))

**배운 것.** 한 PC에서만 깨지는 테스트는 코드보다 환경을 먼저 의심한다. 덤으로, 앱에 건 `connect-src 'none'`이
끼워 넣어진 스크립트의 전송을 실제로 막고 있었다. "입력은 브라우저 밖으로 나가지 않는다"는 화면의 약속을
브라우저가 지켜 준 셈이다.

## 2. 반올림된 숫자에서 거꾸로 계산하지 않는다

**상황.** 논문 예시(학습에 없던 공격을 시험한 XGBoost)를 앱에 넣으려면 혼동행렬 네 칸이 필요했다. 원고에는
반올림된 지표(Accuracy 0.6299, FPR 0.0002 등)만 있다.

**문제.** 원고의 FPR 0.0002로 오탐을 거꾸로 계산하면 약 75건이다(정상 375,518건 × 0.0002). 제출한 연구의 DAY-2
보고서에 있는 정확한 값은 86건이다. 반올림 값에서 만든 숫자를 "논문 수치"로 보여 줄 뻔했다. 설계를 검토하던
단계에서 잡았다([`planning/CLAUDE-CARD1-ANSWER.md`](planning/CLAUDE-CARD1-ANSWER.md)).

**해결.** 예시는 보고서의 정확한 혼동행렬 두 개를 근거 P09로 쓴다.

| 분할 | TN · FP · FN · TP |
|---|---|
| 계층화 무작위 | 418,934 · 362 · 220 · 84,955 |
| 미관측 공격 스트레스 테스트 | 375,432 · 86 · 220,628 · 160 |

이 두 행렬에서 원고의 반올림 지표 8개(Accuracy·Macro F1·공격 Recall·FPR × 두 분할)가 그대로 나오는지 단위
테스트로 확인한다([`tests/unit/metrics.test.ts`](tests/unit/metrics.test.ts)).

**비슷한 실수 하나 더.** 정확도 0.6299와 "모든 흐름을 정상으로 예측한 기준선" 0.6297의 차이를 테스트 이름에
"0.0002 차이"라고 적었다. 이것도 반올림한 두 값의 차일 뿐이다. 실제 차이는 596,306건 가운데 74건
(TP 160 − FP 86), 약 0.00012다. 테스트 이름과 검사를 "74건(TP−FP)"으로 고쳤다. 화면은 기준선을 "약 0.6297"로
쓰고, 판정 기준이 아니라 정확도를 읽기 위한 파생값이라고 밝힌다.

**배운 것.** 화면의 숫자는 모두 원자료로 되돌아갈 수 있어야 한다. 반올림 값끼리 계산해서는 새 사실이 생기지 않는다.

## 3. 공개한 것과 제출한 것이 같다는 증명 — 재현 가능한 ZIP

**요구.** 공개 URL과 함께 비밀번호 없는 ZIP을 낸다. ZIP에는 소스·lockfile·README·예시 데이터·테스트·라이선스만
넣고, 설치물·Git 기록·로그·환경 파일·작업 PC 경로는 넣지 않는다. 새 임시 폴더에서 README대로 실행돼야 한다.

**부딪힌 것.**

1. 안전 검사(`npm run check`)가 파일 목록을 `git ls-files`로 얻었다. Git 기록이 없는 ZIP 폴더에서는 돌 수 없었다.
2. Windows Git의 `core.autocrlf=true` 때문에 `git archive`가 줄 끝을 CRLF로 바꿨다. 같은 커밋이라도 PC 설정에
   따라 ZIP 바이트가 달라질 수 있었다.
3. ZIP을 만든 뒤 증거를 남기다가 ZIP에 들어가는 테스트 파일 한 줄을 고쳤다. 제출할 ZIP이 최신 코드와 달라졌다.
4. 과제 기록(`planning/`)에는 작업 폴더 경로가 적혀 있다.

**해결.**

- [`scripts/make-release-zip.mjs`](scripts/make-release-zip.mjs)는 명시한 허용 목록만
  `git -c core.autocrlf=false archive`로 묶는다. 파일 시각은 커밋 시각으로 고정되고, 허용 목록 안에
  `node_modules`·`.env`·로그가 끼면 멈춘다.
- 안전 검사는 Git이 없으면 설치물·빌드 폴더를 빼고 폴더를 직접 훑는다. 사용자 폴더 경로(`드라이브:\Users\…`)
  검사도 더했다.
- 순서를 고정했다. 코드를 커밋하고, 그 커밋으로 ZIP을 만들고, 해시는 다음 문서 커밋에 적는다. 문서는 ZIP에
  들어가지 않으므로 적어 둔 해시가 계속 맞다.

**결과.** 같은 커밋에서 두 번 만들면 SHA-256이 같다. 최종 ZIP을 새 임시 폴더에 풀어 확인했다.

- `npm ci`, `npm run build`, `npm run preview`(HTTP 200)
- 안전 검사 11개, 단위·컴포넌트 75개, 브라우저 20개

빌드 자산 이름(내용 해시)이 공개 주소와 같다. 제출한 묶음이 공개된 앱을 그대로 만든다는 뜻이다.
([`evidence/card-4.md`](evidence/card-4.md), [`evidence/card-5.md`](evidence/card-5.md))

**배운 것.** "재현된다"는 말은 해시로 증명한다.

## 4. 내보내는 산출물을 직접 검사한다

**기준.** 제출물 어디에도 본인 외 다른 사람의 실명·연락처와 비밀값이 없어야 한다(BRB-C12). 예외 조항이 없다.

**네 번 걸렸다.**

1. 브라우저 저장소 검사가 앱 코드의 **주석**에 적힌 `localStorage` 한 단어를 잡았다. 검사기를 느슨하게 만들지 않고
   주석을 고쳤다.
2. 검사기가 실제로 잡는지 보려고 가짜 이메일·전화번호를 넣은 임시 파일로 시험했다. 그 값을 증거 문서에 그대로
   적었더니, 다음 카드에서 `npm run check`가 그 문서를 잡았다. 문서 표기를 "`…@example.com` 형식"처럼 바꿨다.
3. 전체 제출물 검사에서 앱 화면과 README의 데이터셋 인용에 저자 세 명의 실명이 나왔다. 학술 인용이지만 기준에
   예외가 없다. 저자 이름을 빼고 논문을 하나로 가리키는 제목·학회·쪽·연도·DOI를 남겼다. 저자까지 적은 전체
   인용은 10번 논문 참고문헌 [1]에 있다고 적었다. 인용에 DOI가 있고 "이니셜+성" 표기가 없는지 단위 테스트로
   막았다([`3dd6274`](https://github.com/myeongjundev/explainsoc/commit/3dd6274)).
4. 그 테스트의 주석에 막으려는 표기의 예로 실제 저자 이름 둘을 적었다. 저장소가 아니라 **풀어 본 ZIP**을 검색하다
   잡았다. 형식만 보이는 가짜 예("A. B. Surname")로 바꿨다([`2e7bd73`](https://github.com/myeongjundev/explainsoc/commit/2e7bd73)).

**결과.** 공개 배포본 5개 파일, ZIP, 저장소 전체에서 비밀값·이메일·전화번호·주민번호·저자 이름이 0건이다.

**배운 것.** 검사기를 시험한 값은 문서와 주석으로 샌다. 저장소보다 실제로 내보내는 산출물(배포본·ZIP)을 검사해야
새는 것을 잡는다. 걸렸을 때 검사기를 느슨하게 만들지 않는다.

## 5. 375px 화면과 스크린 리더

- **가로 넘침.** 375px에서 연구 범위 화면의 그리드 칸 안의 표 때문에 페이지 전체가 옆으로 밀렸다. 그리드 칸은
  기본 최소 너비가 `auto`라 안쪽 내용의 너비 밑으로 줄어들지 않는다. 그리드 자식에 `min-width: 0`을 줬다.
  375·768·1440 폭에서 가로 스크롤이 없는지 E2E로 확인한다.
- **그래프 축.** 기준선 그래프에서 막대의 이름·값 칸이 글자 길이에 따라 달라, 0·0.5·1 눈금이 막대와 어긋났다.
  막대와 축이 같은 CSS 변수(`--bar-label`, `--bar-value`)로 칸을 나누게 했다. 축은 0부터 시작한다. 작은 차이를
  키워 보이지 않게 하려는 것이다.
- **링크 이름.** 외부 링크의 숨긴 설명 앞에 공백이 없어 스크린 리더가 "논문 저장소(외부 링크, 새 탭)"를 붙여
  읽었다. 명시적 공백을 넣었다. 접근성 트리로 확인했고, axe(WCAG 2.2 AA)를 네 화면에 돌려 위반 0건이다.
- **라이브 영역.** 카드 뒤집기는 바뀐 내용을 스크린 리더에 읽어 주려고 앞면과 같은 문장을 한 번 더 둔다.
  테스트 선택자가 두 곳을 모두 잡아 실패해서, 선택자 범위를 앞면으로 좁혔다. 문장이 두 번 있는 것은 의도다.

## 6. 빌드가 지운 라이선스 고지

배포본 JS에서 "Copyright"를 찾으니 0건이었다. 번들러가 주석을 지워 React(MIT)의 저작권 고지가 배포본에서
빠져 있었다. MIT는 모든 복사본에 고지를 포함하라고 한다.

Vite 8의 `build.license`로, 묶인 의존성의 라이선스 전문을 함께 내보내게 했다. 그런데 기본 경로
`.vite/license.md`는 GitHub Pages 업로드 액션이 점으로 시작하는 파일을 기본으로 빼는 탓에(`--exclude=.[^/]*`)
조용히 사라질 뻔했다. 배포본 맨 위의 `third-party-licenses.md`로 옮겼고, 공개 주소에서 200으로 열린다.

## 7. Node 버전 표기

README에 "Node.js 20.19 이상"이라고 적었는데, 이것은 Vite만의 요구였다. vitest 5.0.1은 `^22.12 || ^24 || >=26`,
jsdom 30.1.0은 `^22.22.2 || ^24.15.0 || >=26`을 요구하므로 Node 20에서는 테스트가 돌지 않는다. `engines`와
README를 고치고, CI는 Node 24로 돌린다.

## 8. 포트폴리오 카드에서 핵심 문장이 잘렸다

12번 사이트의 대표작 칸은 데스크톱에서 세로(417×500)이고, 그림을 `object-fit: cover`로 가운데 기준으로 자른다.
결과 화면 가로 캡처(1200×750)를 넣었더니 "공격 220,788건 가운데 탐지 160건"이 "운데 탐지 160건"으로 잘렸다.

"숫자 뒤집어 보기" 칸만 2배율로 찍어 3:4(900×1200)로 잘랐다. 데스크톱 카드, 모바일 카드, 왼쪽 카드 묶음 세
곳에서 문장이 온전히 보이는지 공개 사이트 스크린샷으로 확인했다. 그림은 들어갈 그릇에 넣어 본 뒤 고른다.

## 9. 다른 PC에서 올린 커밋

12번 저장소를 고치기 시작했는데 로컬이 원격보다 13커밋 뒤였다. 다른 PC(학원)에서 푸시한 커밋들이었다. 편집을
멈추고 바꾼 내용을 따로 저장한 뒤 되돌리고 받아 왔다. 커밋 전이라 잃은 것은 없다. 그 뒤로는 어느 저장소든
만지기 전에 `git fetch`와 `git status -sb`부터 한다.

---

## 작은 것들

| 문제 | 해결 |
|---|---|
| 설계의 "지표가 있음"이 사용자가 적은 값인지, 혼동행렬에서 계산한 값인지 두 가지로 읽혔다(R03·R04·R10) | 설계가 적어 둔 예시 A의 기대 결과(R04가 뜬다)에 맞는 해석을 골랐다. 구현 체크리스트에 근거와 함께 적었다 |
| 화면에 쓰면 안 되는 표현("시간 분할", "미래 성능")이 논문 원문 인용 안에 있다 | 원문은 고치지 않는다. 문구 검사는 원문 인용을 빼고 돌리고, 그 인용이 금지 표현을 부정하는 문장("…이 아니라")인지 따로 확인한다 |
| 결과 화면에 설계에 없는 "평가 조건 바꾸기" 버튼을 넣으려 했다 | 단계 표시로 이미 갈 수 있고 설계에 없는 기능이라 뺐다 |
| 클립보드 쓰기 권한이 없는 브라우저가 있다 | 앱의 대체 경로(질문 목록 선택 + 안내 문구)가 동작한다. 권한이 있는 Chrome에서는 E2E가 클립보드 내용까지 확인한다 |
| 공개 주소의 출처가 같은 계정의 다른 Pages 사이트와 같다(다른 사이트의 저장소 키가 보였다) | 입력을 메모리에만 두는 설계라 다른 사이트가 읽을 것이 없다 |
| GitHub Pages에서는 틀 안에 넣기를 막는 `frame-ancestors`를 걸 수 없다(메타 태그 CSP는 무시한다) | 로그인·저장·전송이 없어 틀에 넣어 속여도 얻을 것이 없으므로 받아들이고 기록했다 |
| `tsc -b`와 `noEmit` 조합 | 타입 검사만 하는 `tsc --noEmit`으로 바꿨다 |
| jsdom에 `window.scrollTo`가 없어 테스트 로그에 오류가 찍혔다 | 테스트 준비 파일에서 대체 함수를 둔다 |
| 숨겨진 브라우저 창에서는 지연 로딩 이미지가 불리지 않는다 | 화면 확인은 Playwright 스크린샷으로 했다 |

## 배운 것

- 한 PC에서만 깨지면 코드보다 환경을 먼저 본다. 검사 환경도 검사 대상이다.
- 화면의 숫자는 원자료로 되돌아갈 수 있어야 한다. 반올림 값에서 거꾸로 만들지 않는다.
- "재현된다"는 해시로 증명한다.
- 내보내는 산출물을 직접 검사한다. 걸렸다고 검사기를 느슨하게 만들지 않는다.
- 들어갈 자리에서 확인한다. 화면 폭, 스크린 리더, 포트폴리오 카드의 그림 칸이 그렇다.
